# v1.7.1 — Renk (L*a*b*) Paneli Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** L*, a*, b*'yi sidebar'da 3 ayrı parametre yerine tek bir "Renk (L*a*b*)" girişi altında, birleşik veri girişi + 3 bağımsız I-MR kartı + görsel swatch önizlemesiyle tek bir sayfada göstermek — istatistiksel motoru (3 bağımsız I-MR serisi, ΔE YOK) değiştirmeden.

**Architecture:** Mevcut `tab_data`/`tab_chart` gövdeleri (app.py'de sırasıyla 1318-1870 ve 1871-2816 satırları) tek-aktif-parametre varsayımına sıkı bağlı ~1500 satırlık prosedürel kod — bunları değiştirmeden, önce (regresyonsuz) birer fonksiyona sarmalıyoruz (`render_generic_data_entry_tab()` / `render_generic_chart_tab()`), sonra bu fonksiyonların çağrıldığı yere `active_parameter == "L*"` koşuluyla YENİ bir "Renk Paneli" dalı ekliyoruz. Renk Paneli kendi bağımsız session_state modelini (`color_lab_samples` — L/a/b üçlüsünü birlikte tutan tek liste) kullanır, mevcut `subgroups` modeline dokunmaz. Yeni saf mantık (`lab_to_hex`, örnek ekleme/silme) `src/color_lab.py`'ye (Streamlit'ten bağımsız, `qc_converters.py` ile aynı ayrım deseninde) yazılır.

**Tech Stack:** Python, Streamlit, pytest, matplotlib (mevcut `style_chart()` yeniden kullanılır) — mevcut stack, yeni bağımlılık yok.

**Spec:** `METHODOLOGY.md` → "v1.7.1 — Renk (L*a*b*) Paneli (UI birleştirme, ΔE DEĞİL)" bölümü. ΔE ve tüm türetilmiş renk-farkı metrikleri bu plandan KESİN KAPSAM DIŞI — "Kapsam dışı — ΔE KESİN, dışarıda" kararı bu planla YENİDEN AÇILMIYOR.

## Global Constraints

- **ΔE YOK:** L*, a*, b* arasında hiçbir türetilmiş/birleşik metrik hesaplanmaz. Üçü DAİMA bağımsız 3 I-MR serisi olarak kalır.
- **`lab_to_hex()` karar verici DEĞİLDİR:** sadece görsel swatch üretir, LSL/USL/Cpk hesaplamasının hiçbir yerinde kullanılmaz. UI'da "yaklaşık önizleme, D65 varsayımlı, cihazınızın aydınlatıcı/gözlemci ayarı farklıysa gerçek rengi yansıtmayabilir" notu ZORUNLU.
- **v1 kapsamı dar:** CSV import/export, PDF export, demo veri üretici, baseline dondurma, Nelson kuralları bu planda YOK — sadece birleşik giriş formu + 3 I-MR grafiği + Cpk rozeti + swatch.
- **Mevcut kod SIFIR regresyon:** `spc_core.py`'ye hiçbir değişiklik yapılmaz. `tab_data`/`tab_chart`'ın mevcut davranışı (L*/a*/b* dışındaki TÜM parametreler için) birebir korunur — Task 2/3'teki fonksiyon-sarmalama adımlarından sonra mevcut 258 testin TAMAMI değişmeden geçmelidir.
- **a*/b* PARAMETER_CONFIG kayıtları SİLİNMEZ:** sadece `PARAMETER_CATEGORIES`'deki "optik" grubunun sidebar listesinden çıkarılır (artık ayrı seçilemezler) — Renk Paneli kodu, varsayılan LSL/USL/physical_bounds değerlerini bu kayıtlardan okumaya devam eder.

---

### Task 1: `color_lab.py` — `lab_to_hex()` saf dönüşüm fonksiyonu

**Files:**
- Create: `src/color_lab.py`
- Test: `tests/test_color_lab.py`
- Create: `validation/optics/lab_to_hex_reference.csv`

**Interfaces:**
- Consumes: yok
- Produces: `lab_to_hex(l_star: float, a_star: float, b_star: float) -> str` — `"#rrggbb"` formatında hex string döner.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_color_lab.py (yeni dosya)
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

from color_lab import lab_to_hex


def test_lab_to_hex_black_is_mathematical_identity():
    # L*=0,a*=0,b*=0 CIE Lab->XYZ donusumunde XYZ=(0,0,0)'a esittir -
    # bu bir olcum degil, formulun kendi tanimindan gelen matematiksel
    # ozdeslik (bkz. docstring), bu yuzden ayri bir kaynak dogrulamasi
    # gerektirmez.
    assert lab_to_hex(0.0, 0.0, 0.0) == "#000000"


def test_lab_to_hex_white_is_mathematical_identity():
    # L*=100,a*=0,b*=0 -> D65 referans beyaz noktasinin kendisi -> (255,255,255).
    assert lab_to_hex(100.0, 0.0, 0.0) == "#ffffff"


def test_lab_to_hex_returns_lowercase_six_digit_hex():
    result = lab_to_hex(65.0, 10.0, 20.0)
    assert result.startswith("#")
    assert len(result) == 7
    assert result == result.lower()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_color_lab.py -v`
Expected: FAIL with "ModuleNotFoundError: No module named 'color_lab'"

- [ ] **Step 3: Write minimal implementation**

```python
# src/color_lab.py (yeni dosya)
"""Renk (L*a*b*) Paneli - saf mantik katmani (Streamlit'ten bagimsiz,
qc_converters.py ile ayni ayrim deseninde).

lab_to_hex(): CIE L*a*b* (D65 aydinlatici, 2 derece gozlemci varsayimi)
-> sRGB hex donusumu. KARAR VERICI DEGILDIR - sadece gorsel onizleme
(swatch) icindir, LSL/USL/Cpk hesaplamasinin hicbir yerinde kullanilmaz.

Kaynak: CIE 1976 L*a*b* standart donusum formulleri (Colorimetry:
Understanding the CIE System, Schanda ed., Wiley 2007, Ch. 3) + sRGB
gamma companding (IEC 61966-2-1). Referans beyaz nokta D65:
Xn=95.047, Yn=100.0, Zn=108.883.
"""

_EPS = 216.0 / 24389.0
_KAPPA = 24389.0 / 27.0
_XN, _YN, _ZN = 95.047, 100.0, 108.883


def lab_to_hex(l_star: float, a_star: float, b_star: float) -> str:
    fy = (l_star + 16.0) / 116.0
    fx = fy + a_star / 500.0
    fz = fy - b_star / 200.0

    fx3, fz3 = fx ** 3, fz ** 3
    xr = fx3 if fx3 > _EPS else (116.0 * fx - 16.0) / _KAPPA
    yr = fy ** 3 if l_star > _KAPPA * _EPS else l_star / _KAPPA
    zr = fz3 if fz3 > _EPS else (116.0 * fz - 16.0) / _KAPPA

    x, y, z = xr * _XN / 100.0, yr * _YN / 100.0, zr * _ZN / 100.0

    r_lin = 3.2406 * x - 1.5372 * y - 0.4986 * z
    g_lin = -0.9689 * x + 1.8758 * y + 0.0415 * z
    b_lin = 0.0557 * x - 0.2040 * y + 1.0570 * z

    def _gamma(c: float) -> int:
        c = max(0.0, min(1.0, c))
        srgb = 12.92 * c if c <= 0.0031308 else 1.055 * (c ** (1 / 2.4)) - 0.055
        return max(0, min(255, round(srgb * 255)))

    r, g, b = _gamma(r_lin), _gamma(g_lin), _gamma(b_lin)
    return f"#{r:02x}{g:02x}{b:02x}"
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_color_lab.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Create the worked-example validation CSV**

```csv
# validation/optics/lab_to_hex_reference.csv
l_star,a_star,b_star,expected_hex,note
0.0,0.0,0.0,#000000,"Matematiksel ozdeslik - L=0 daima siyaha esitlenir (formul tanimindan)"
100.0,0.0,0.0,#ffffff,"Matematiksel ozdeslik - L=100 D65 referans beyaz noktasinin kendisi"
```

- [ ] **Step 6: Add the CSV-driven test to the validation suite**

```python
# tests/test_validation_suite.py icine eklenir (mevcut import bloguna
# color_lab.lab_to_hex eklenir, dosyanin en ustundeki import listesine)

def test_lab_to_hex_reference_csv():
    import csv
    path = os.path.join(
        os.path.dirname(__file__), "..", "validation", "optics", "lab_to_hex_reference.csv"
    )
    with open(path, newline="", encoding="utf-8") as f:
        rows = [r for r in csv.DictReader(f, ) if not r["l_star"].startswith("#")]
    for row in rows:
        result = lab_to_hex(float(row["l_star"]), float(row["a_star"]), float(row["b_star"]))
        assert result == row["expected_hex"], f"{row}: beklenen {row['expected_hex']}, alinan {result}"
```

- [ ] **Step 7: Run full test suite**

Run: `pytest tests/ -q`
Expected: tum testler PASS (onceki 258 + bu task'ta eklenenler)

- [ ] **Step 8: Commit**

```bash
git add src/color_lab.py tests/test_color_lab.py tests/test_validation_suite.py validation/optics/lab_to_hex_reference.csv
git commit -m "feat: lab_to_hex() CIE L*a*b*->sRGB donusumu (Renk Paneli swatch on izlemesi icin)"
```

---

### Task 2: `tab_data` gövdesini `render_generic_data_entry_tab()`'e sarmala (SIFIR mantık değişikliği)

**Files:**
- Modify: `src/app.py:1318-1870`

**Interfaces:**
- Consumes: yok (mevcut modül-seviyesi değişkenleri okumaya devam eder — `st.session_state`, `param_config`, vb.)
- Produces: `render_generic_data_entry_tab() -> None` — çağrıldığında `tab_data` bloğunun ŞU ANKİ (Task 2 öncesi) davranışının BİREBİR aynısını üretir.

- [ ] **Step 1: Satır 1318'deki `with tab_data:` satırını ve altındaki gövdeyi (1319-1870) bir fonksiyona dönüştür**

Mekanik işlem (mantık değişikliği YOK):
1. `with tab_data:` satırını `def render_generic_data_entry_tab() -> None:` ile değiştir.
2. Altındaki gövdenin (mevcut 1319-1870 satırları) girintisi zaten `with` bloğu içindeki girintiyle (4 boşluk) aynı olduğu için EK bir girinti eklemeye GEREK YOK — sadece `with tab_data:` -> `def render_generic_data_entry_tab() -> None:` satır değişikliği yeterli (Python'da fonksiyon gövdesi de aynı 4 boşluk girintisini kullanır).
3. Fonksiyon tanımının HEMEN altına, docstring olarak şunu ekle:

```python
def render_generic_data_entry_tab() -> None:
    """SEKME 1: Veri Girisi - tek-aktif-parametre (L*/a*/b* HARIC tum
    parametreler) icin veri girisi formu, demo veri, CSV import, Excel
    yapistir. v1.7.1 (Renk Paneli) oncesi bu kod dogrudan `with tab_data:`
    icindeydi - Renk Paneli icin ayri bir dal eklenebilmesi icin (Task 5)
    fonksiyona sarmalandi, ic mantik degismedi."""
```

4. Fonksiyon tanımının SONUNA (dosyada `with tab_chart:` satırından hemen önce), fonksiyonu normal akışta çağıran şu satırları ekle (Task 5'te bu çağrı koşullu hale gelecek, şimdilik koşulsuz):

```python
with tab_data:
    render_generic_data_entry_tab()
```

- [ ] **Step 2: Run full test suite to verify zero regression**

Run: `pytest tests/ -q`
Expected: 258 passed (Task 1'de eklenenlerle birlikte hepsi) — hiçbir testin davranışı değişmemiş olmalı, çünkü bu SADECE bir sarmalama.

- [ ] **Step 3: Commit**

```bash
git add src/app.py
git commit -m "refactor: tab_data govdesini render_generic_data_entry_tab() fonksiyonuna sarmala (mantik degisikligi yok)"
```

---

### Task 3: `tab_chart` gövdesini `render_generic_chart_tab()`'e sarmala (SIFIR mantık değişikliği)

**Files:**
- Modify: `src/app.py:1871-2816`

**Interfaces:**
- Consumes: yok
- Produces: `render_generic_chart_tab() -> None` — Task 3 öncesi `tab_chart`'ın davranışının birebir aynısı.

- [ ] **Step 1: Aynı mekanik işlemi `tab_chart` için tekrarla**

1. `with tab_chart:` satırını `def render_generic_chart_tab() -> None:` ile değiştir (docstring: "SEKME 2: X-bar/R Chart & Cpk - L*/a*/b* HARIC tum parametreler icin. v1.7.1 oncesi dogrudan `with tab_chart:` icindeydi, Renk Paneli dali icin fonksiyona sarmalandi, ic mantik degismedi.").
2. Gövde girintisi değişmez (aynı Task 2 mantığı).
3. Fonksiyon tanımının SONUNA (`def render_totox_gauge` satırından hemen önce):

```python
with tab_chart:
    render_generic_chart_tab()
```

- [ ] **Step 2: Run full test suite to verify zero regression**

Run: `pytest tests/ -q`
Expected: tum testler PASS, hicbir degisiklik yok.

- [ ] **Step 3: Commit**

```bash
git add src/app.py
git commit -m "refactor: tab_chart govdesini render_generic_chart_tab() fonksiyonuna sarmala (mantik degisikligi yok)"
```

---

### Task 4: Renk Paneli session-state modeli + saf yardımcı fonksiyonlar

**Files:**
- Modify: `src/color_lab.py`
- Test: `tests/test_color_lab.py`

**Interfaces:**
- Consumes: yok
- Produces:
  - `append_color_sample(samples: list[dict], l: float, a: float, b: float) -> list[dict]` — yeni `{"L": l, "a": a, "b": b}` girdisini ekleyip YENİ listeyi döner (in-place mutate etmez).
  - `color_samples_to_series(samples: list[dict]) -> tuple[list[float], list[float], list[float]]` — `(L_values, a_values, b_values)` üç ayrı liste döner.

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_color_lab.py icine eklenir

from color_lab import append_color_sample, color_samples_to_series


def test_append_color_sample_adds_entry_without_mutating_original():
    original = [{"L": 60.0, "a": 5.0, "b": 10.0}]
    result = append_color_sample(original, 61.0, 5.5, 10.5)
    assert len(original) == 1
    assert len(result) == 2
    assert result[1] == {"L": 61.0, "a": 5.5, "b": 10.5}


def test_color_samples_to_series_splits_into_three_lists():
    samples = [
        {"L": 60.0, "a": 5.0, "b": 10.0},
        {"L": 61.0, "a": 5.5, "b": 10.5},
    ]
    l_vals, a_vals, b_vals = color_samples_to_series(samples)
    assert l_vals == [60.0, 61.0]
    assert a_vals == [5.0, 5.5]
    assert b_vals == [10.0, 10.5]


def test_color_samples_to_series_empty_input():
    assert color_samples_to_series([]) == ([], [], [])
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/test_color_lab.py -v`
Expected: FAIL with "ImportError: cannot import name 'append_color_sample'"

- [ ] **Step 3: Write minimal implementation**

```python
# src/color_lab.py sonuna eklenir

def append_color_sample(samples: list[dict], l: float, a: float, b: float) -> list[dict]:
    """Yeni bir L*/a*/b* uclusunu (ayni olcume ait) listeye ekler - orijinal
    listeyi DEGISTIRMEZ, yeni bir liste doner (Streamlit session_state
    mutasyon hatalarindan kacinmak icin - bkz. build_bridge_subgroup_entry
    ile ayni desen, kopru sisteminden odunc alindi)."""
    return samples + [{"L": l, "a": a, "b": b}]


def color_samples_to_series(samples: list[dict]) -> tuple[list[float], list[float], list[float]]:
    """Birlesik ornek listesini, spc_core'un beklediği 3 bagimsiz I-MR
    serisine ayirir - L*, a*, b* istatistiksel olarak DAIMA bagimsizdir
    (bkz. METHODOLOGY.md 'v1.7.1' - ΔE YOK)."""
    l_vals = [s["L"] for s in samples]
    a_vals = [s["a"] for s in samples]
    b_vals = [s["b"] for s in samples]
    return l_vals, a_vals, b_vals
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/test_color_lab.py -v`
Expected: PASS (6 passed - Task 1'in 3 testiyle birlikte)

- [ ] **Step 5: Commit**

```bash
git add src/color_lab.py tests/test_color_lab.py
git commit -m "feat: Renk Paneli icin saf veri modeli yardimcilari (append_color_sample, color_samples_to_series)"
```

---

### Task 5: Sidebar wiring — "Renk (L*a*b*)" tek girişi + tab_data/tab_chart dallanması

**Files:**
- Modify: `src/constants.py` (PARAMETER_CATEGORIES "optik" grubu)
- Modify: `src/app.py` (sidebar format_func, tab_data/tab_chart çağrı noktaları)
- Test: `tests/test_food_quality_framework.py`

**Interfaces:**
- Consumes: `render_generic_data_entry_tab()` (Task 2), `render_generic_chart_tab()` (Task 3)
- Produces: sidebar'da "optik" kategorisi artık `["Refraktif Indeks", "L*", "Bulaniklik", "Iletkenlik"]` listeler (a*, b* kaldırıldı — L* artık trio'nun temsilcisi); `active_parameter == "L*"` seçildiğinde ekranda "Renk (L*a*b*)" yazar.

- [ ] **Step 1: `constants.py`'de "optik" kategorisinden a*/b*'yi çıkar**

```python
# src/constants.py - PARAMETER_CATEGORIES icindeki "optik" tuple'i degistir:
(
    "optik", "\U0001F3A8 Optik/Gorunum",
    # a*/b* artik ayri secilemez - "L*" secimi, v1.7.1'den itibaren
    # Renk (L*a*b*) Panelini (ucunu BIRLIKTE gosteren ozel sayfa) temsil
    # eder (bkz. app.py sidebar format_func ve tab_data/tab_chart
    # dallanmasi). a*/b*'nin PARAMETER_CONFIG kayitlari SILINMEDI -
    # Renk Paneli varsayilan LSL/USL/physical_bounds degerlerini
    # oradan okumaya devam eder.
    ["Refraktif Indeks", "L*", "Bulaniklik", "Iletkenlik"],
),
```

- [ ] **Step 2: `app.py`'de `_param_radio_label`'a L* için özel etiket ekle**

`_param_radio_label` fonksiyonunu (satır ~255-262) şu şekilde güncelle:

```python
def _param_radio_label(p: str) -> str:
    display_name = "Renk (L*a*b*)" if p == "L*" else p
    if p == st.session_state.active_parameter:
        return f"{_status_dot} {display_name}"
    return display_name
```

- [ ] **Step 3: `render_last_analysis_card`, PDF başlığı, herhangi bir yerde "L*" ham metniyle kullanıcıya gösterilen diğer noktaları bul ve kontrol et**

Run: `grep -n '"L\*"' src/app.py` — her eşleşmeyi incele. Kullanıcıya DOĞRUDAN gösterilen metin konumlarında (örn. `st.caption(f"...{active_parameter}...")` gibi Renk Paneli dışı genel amaçlı kod) "L*" yerine görünen isim gerekmiyor (bu kod yolları artık Renk Paneli seçiliyken hiç ÇALIŞMAYACAK — Task 5 Step 4'teki dallanma sayesinde). Sadece sidebar radio etiketi (Step 2) kullanıcıya görünür kalan tek yer.

- [ ] **Step 4: `tab_data`/`tab_chart` çağrı noktalarına koşullu dallanma ekle**

Task 2 Step 1.4'te eklenen `with tab_data: render_generic_data_entry_tab()` çağrısını şu şekilde değiştir:

```python
with tab_data:
    if st.session_state.active_parameter == "L*":
        render_color_lab_data_entry_tab()  # Task 6'da tanimlanacak
    else:
        render_generic_data_entry_tab()
```

Task 3 Step 1.3'te eklenen `with tab_chart: render_generic_chart_tab()` çağrısını:

```python
with tab_chart:
    if st.session_state.active_parameter == "L*":
        render_color_lab_chart_tab()  # Task 6'da tanimlanacak
    else:
        render_generic_chart_tab()
```

(Bu adımdan sonra `render_color_lab_data_entry_tab`/`render_color_lab_chart_tab` henuz tanimli olmadigi icin uygulama L*'yi sectiginizde NameError verecektir - bu BEKLENEN bir ara durumdur, Task 6'da cozulur. Bu task'in testi bu iki fonksiyonu cagirmaz, sadece L* DISI parametrelerin hala calistigini dogrular.)

- [ ] **Step 5: `compute_active_parameter_status()`'a Renk Paneli guard'i ekle**

Satır ~174 (`param_cfg = PARAMETER_CONFIG[active_param]`) öncesine ekle:

```python
def compute_active_parameter_status() -> tuple[str, float | None]:
    if len(st.session_state.subgroups) < 2:
        return "gray", None
    active_param = st.session_state.active_parameter
    if active_param == "L*":
        # Renk Paneli kendi ayri veri modelini (color_lab_samples) kullanir,
        # sidebar durum noktasi bu model icin anlamli degil - notr kalir.
        return "gray", None
    param_cfg = PARAMETER_CONFIG[active_param]
    ...  # geri kalani degismez
```

- [ ] **Step 6: Write the regression test**

```python
# tests/test_food_quality_framework.py icine eklenir

def test_optik_category_no_longer_lists_a_star_or_b_star_separately():
    optik = next(params for cat_id, _label, params in PARAMETER_CATEGORIES if cat_id == "optik")
    assert "L*" in optik
    assert "a*" not in optik
    assert "b*" not in optik
    # a*/b* PARAMETER_CONFIG kayitlari SILINMEDI (Renk Paneli hala okur):
    assert "a*" in PARAMETER_CONFIG
    assert "b*" in PARAMETER_CONFIG
```

- [ ] **Step 7: Run full test suite**

Run: `pytest tests/ -q`
Expected: tum testler PASS. (`test_app_render_smoke.py`'deki FOOD_QUALITY_PARAMETER_CONFIG dongusu hala a*/b*'yi test eder ama bu dogrudur - a*/b* konfigurasyonlari hala gecerli, sadece sidebar'da AYRI secilemiyorlar; bu test app.py'yi CALISTIRMIYOR, sadece kaynak yapisini kontrol ediyor, bu yuzden etkilenmez.)

- [ ] **Step 8: Commit**

```bash
git add src/app.py src/constants.py tests/test_food_quality_framework.py
git commit -m "feat: sidebar'da L*/a*/b* icin tek 'Renk (L*a*b*)' girisi + tab dallanma iskeleti"
```

---

### Task 6: Renk Paneli — birleşik veri girişi + 3 I-MR grafiği + Cpk + swatch

**Files:**
- Modify: `src/app.py`

**Interfaces:**
- Consumes: `append_color_sample`, `color_samples_to_series` (Task 4), `lab_to_hex` (Task 1), `compute_moving_ranges`, `compute_imr_limits`, `compute_cpk`, `is_spec_valid` (mevcut `spc_core` import'u), `style_chart`, `cpk_capability_badge`, `render_capability_card` (mevcut app.py fonksiyonları)
- Produces: `render_color_lab_data_entry_tab() -> None`, `render_color_lab_chart_tab() -> None`

- [ ] **Step 1: `color_lab_samples` session-state başlatmasını ekle**

Dosyanın başındaki `if "subgroups" not in st.session_state:` bloğunun hemen altına:

```python
if "color_lab_samples" not in st.session_state:
    st.session_state.color_lab_samples = []  # list of dict: {"L", "a", "b"}
```

- [ ] **Step 2: `render_color_lab_data_entry_tab()` fonksiyonunu yaz**

`render_generic_data_entry_tab()` tanımının hemen üstüne ekle:

```python
def render_color_lab_data_entry_tab() -> None:
    """Renk (L*a*b*) Paneli - SEKME 1: birlesik veri girisi. v1.7.1 v1
    kapsami: sadece manuel giris (CSV import/demo veri YOK - bkz.
    METHODOLOGY.md 'v1.7.1' v1 kapsam notu)."""
    st.subheader("\U0001F3A8 Renk (L*a*b*) - Birlesik Olcum Girisi")
    st.caption(
        "L*, a*, b* ayni spektrofotometre/kolorimetre okumasindan cikar - "
        "ucu birlikte, tek formda girilir. Istatistiksel olarak DAIMA "
        "bagimsiz 3 I-MR serisi olarak izlenir (ΔE hesaplanmaz)."
    )
    with st.form("color_lab_entry_form"):
        c1, c2, c3 = st.columns(3)
        l_val = c1.number_input("L* (0-100)", min_value=0.0, max_value=100.0, value=65.0, step=0.1)
        a_val = c2.number_input("a* (-128/+127)", min_value=-128.0, max_value=127.0, value=10.0, step=0.1)
        b_val = c3.number_input("b* (-128/+127)", min_value=-128.0, max_value=127.0, value=20.0, step=0.1)
        submitted = st.form_submit_button("Olcumu Ekle")
        if submitted:
            st.session_state.color_lab_samples = append_color_sample(
                st.session_state.color_lab_samples, l_val, a_val, b_val
            )
            st.success(f"Eklendi: L*={l_val:g}, a*={a_val:g}, b*={b_val:g}")

    n_samples = len(st.session_state.color_lab_samples)
    st.caption(f"Toplam {n_samples} olcum.")
    if n_samples > 0:
        if st.button("Tum olcumleri temizle", key="color_lab_clear"):
            st.session_state.color_lab_samples = []
            st.rerun()
        st.dataframe(st.session_state.color_lab_samples, width="stretch")
```

- [ ] **Step 3: `render_color_lab_chart_tab()` fonksiyonunu yaz**

```python
def render_color_lab_chart_tab() -> None:
    """Renk (L*a*b*) Paneli - SEKME 2: 3 bagimsiz I-MR karti + swatch.
    v1.7.1 v1 kapsami: baseline dondurma/Nelson kurallari YOK - her
    render'da TUM veriyle canli I-MR limitleri hesaplanir (bkz.
    METHODOLOGY.md 'v1.7.1' v1 kapsam notu)."""
    samples = st.session_state.color_lab_samples
    if len(samples) < 2:
        render_empty_state("\U0001F3A8", "Grafik icin en az 2 olcum gerekli. Once Veri Girisi sekmesinden ekleyin.")
        return

    l_vals, a_vals, b_vals = color_samples_to_series(samples)
    last = samples[-1]
    swatch_hex = lab_to_hex(last["L"], last["a"], last["b"])
    sc1, sc2 = st.columns([1, 4])
    with sc1:
        st.markdown(
            f'<div style="width:60px;height:60px;border-radius:8px;'
            f'background-color:{swatch_hex};border:1px solid #888;"></div>',
            unsafe_allow_html=True,
        )
    with sc2:
        st.caption(
            f"Son olcum onizlemesi: {swatch_hex} (L*={last['L']:g}, a*={last['a']:g}, b*={last['b']:g}). "
            "⚠️ Yaklasik onizleme, D65 aydinlatici varsayimiyla hesaplanir - "
            "cihazinizin aydinlatici/gozlemci ayari farkliysa gercek rengi yansitmayabilir. "
            "Karar verici DEGILDIR."
        )

    axis_configs = [
        ("L*", l_vals, FOOD_QUALITY_PARAMETER_CONFIG["L*"]),
        ("a*", a_vals, FOOD_QUALITY_PARAMETER_CONFIG["a*"]),
        ("b*", b_vals, FOOD_QUALITY_PARAMETER_CONFIG["b*"]),
    ]
    cols = st.columns(3)
    for col, (axis_name, values, axis_cfg) in zip(cols, axis_configs):
        with col:
            st.markdown(f"**{axis_name}**")
            x_bar = sum(values) / len(values)
            mr_list = compute_moving_ranges(values)
            mr_bar = sum(mr_list) / len(mr_list)
            lsl, usl = axis_cfg["default_lsl"], axis_cfg["default_usl"]
            if is_spec_valid(False, lsl, usl):
                cpk = compute_cpk(x_bar, mr_bar, 2, lsl, usl, one_sided=False)
                badge_label, badge_color, _ = cpk_capability_badge(cpk, True)
                st.metric(f"{axis_name} Cpk", f"{cpk:.2f}" if cpk not in (float("inf"), float("-inf")) else str(cpk), badge_label)
            else:
                st.caption("Gecersiz spesifikasyon (LSL >= USL)")
            fig, ax = plt.subplots(figsize=(3.2, 2.4))
            imr = compute_imr_limits(x_bar, mr_bar)
            ax.plot(range(1, len(values) + 1), values, marker="o", markersize=3)
            ax.axhline(imr.ucl_i, color="red", linestyle="--", linewidth=0.8)
            ax.axhline(imr.lcl_i, color="red", linestyle="--", linewidth=0.8)
            ax.axhline(x_bar, color="gray", linestyle=":", linewidth=0.8)
            style_chart(fig, ax, dark=(st.session_state.get("chart_theme") == "Koyu"))
            st.pyplot(fig)
            plt.close(fig)
```

- [ ] **Step 4: `constants.py`'den `FOOD_QUALITY_PARAMETER_CONFIG` importunun app.py'de zaten mevcut olduğunu doğrula, `color_lab` importunu ekle**

`app.py`'nin en üstündeki `from qc_converters import ...` satırının hemen altına:

```python
from color_lab import append_color_sample, color_samples_to_series, lab_to_hex
```

- [ ] **Step 5: Manuel doğrulama (uygulamayı çalıştırıp elle test et)**

Run: `streamlit run src/app.py` (yerel makinede)
1. Sidebar'da "Optik/Görünüm" kategorisini aç, "Renk (L*a*b*)" seç.
2. Veri Girişi sekmesinde 3-4 ölçüm ekle (farklı L/a/b değerleriyle).
3. Chart & Cpk sekmesine geç — 3 grafik + Cpk rozetleri + swatch görünmeli.
4. pH gibi başka bir parametreye geçip geri dönerek eski akışın hâlâ çalıştığını doğrula.

- [ ] **Step 6: Run full test suite**

Run: `pytest tests/ -q`
Expected: tum testler PASS.

- [ ] **Step 7: Commit**

```bash
git add src/app.py
git commit -m "feat: Renk (L*a*b*) Paneli - birlesik giris + 3 bagimsiz I-MR karti + swatch"
```

---

### Task 7: Regresyon güvence testi + versiyon rozeti

**Files:**
- Modify: `tests/test_app_render_smoke.py`
- Modify: `src/app.py` (versiyon rozeti)

**Interfaces:**
- Consumes: yok
- Produces: yok (sadece test + rozet)

- [ ] **Step 1: Write the regression test**

```python
# tests/test_app_render_smoke.py icine eklenir

def test_a_star_and_b_star_config_still_valid_but_not_in_sidebar_categories():
    # Task 5'in sonucu: a*/b* PARAMETER_CONFIG'de KALDI (Renk Paneli
    # okuyor) ama hicbir PARAMETER_CATEGORIES grubunda AYRI listelenmiyor.
    from constants import PARAMETER_CATEGORIES
    all_categorized = {p for _cat_id, _label, params in PARAMETER_CATEGORIES for p in params}
    assert "a*" not in all_categorized
    assert "b*" not in all_categorized
    assert "L*" in all_categorized
    assert "a*" in FOOD_QUALITY_PARAMETER_CONFIG
    assert "b*" in FOOD_QUALITY_PARAMETER_CONFIG
```

- [ ] **Step 2: Run test to verify it passes**

Run: `pytest tests/test_app_render_smoke.py -v`
Expected: PASS

- [ ] **Step 3: Versiyon rozetini güncelle**

`src/app.py`'nin en altındaki `st.caption(f"SPC FoodLab v1.7 · ...")` satırını `v1.7.1` yap.

- [ ] **Step 4: Run full test suite one final time**

Run: `pytest tests/ -q`
Expected: tum testler PASS.

- [ ] **Step 5: Commit**

```bash
git add tests/test_app_render_smoke.py src/app.py
git commit -m "test: a*/b* config-hala-gecerli/sidebar-hala-yok regresyon testi + versiyon rozeti v1.7.1"
```
