# v1.7 Faz 3 — Termal Letalite (F₀) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SPC FoodLab'e, Bigelow/Ball formülüyle termal letalite (F₀) hesaplayan bir QC Veri Dönüştürücü eklemek — Totox'un izlediği aynı desenle (bağımsız bir köprü config'i, herhangi bir mevcut I-MR parametresine bağlanabilir, mevcut Viskozite parametresine ÖZEL olarak bağlı DEĞİL).

**Architecture:** `src/qc_converters.py`'ye yeni bir saf-mantık formülü (`thermal_lethality_f0`) eklenir — bir zaman-sıcaklık profilinden (n adet sıcaklık okuması + sabit örnekleme aralığı) tek bir F₀ değeri hesaplar. `src/constants.py`'ye, Totox'unkiyle birebir aynı mimari desende (`TOTOX_BRIDGE_PARAMETER_CONFIG`), bağımsız bir `F0_BRIDGE_PARAMETER_CONFIG` eklenir — `PARAMETER_CONFIG`'e KATILMAZ, LSL=3.0 dk (FDA 21 CFR 113 kaynaklı) sadece bilgilendirme mesajında kullanılır. `src/app.py`'de yeni panel, mevcut `render_bridge_widget()`'i (Faz 1/2'den, hiçbir değişiklik gerekmez) Totox'un yaptığı gibi "uygulamadaki TÜM I-MR parametreleri" hedef listesiyle çağırır.

**Tech Stack:** Python, Streamlit, pytest, pandas (validation CSV okuma) — mevcut stack, yeni bağımlılık yok.

**Spec:** `METHODOLOGY.md` → "v1.7 — QC Veri Dönüştürücüler (fazlı)" bölümü, "Faz 3 — Karmaşık Validasyon Gerektirenler (sadece F₀)" alt maddesi. Bostwick/Viskozite Sıcaklık Normalizasyonu bu plandan KAPSAM DIŞI — METHODOLOGY.md'nin "Kalıcı hariç tutulanlar" bölümünde kalıcı olarak reddedildi (standart bir düzeltme katsayısı yok, ürüne özgü deneysel Ea gerektiriyor, SPC pre-processor kimliğine uymuyor).

## Global Constraints

- **Altın Kural:** F₀'ın çıktısı SPC kontrol kartının Y eksenine (mevcut bir I-MR parametresinin `subgroups` verisine) bağlanmalıdır.
- **F₀'ın statüsü (Totox'la birebir aynı desen):** Tam bir `FOOD_QUALITY_PARAMETER_CONFIG` üyesi DEĞİLDİR, `PARAMETER_CONFIG`'e KATILMAZ. Ürün bazlı LSL/USL araştırması gerekmez çünkü F₀'ın FDA 21 CFR 113'ten gelen TEK, evrensel bir LSL'i (3.0 dakika) vardır. Köprünün amacı sadece ham F₀ değerini, kullanıcının seçtiği herhangi bir I-MR parametresinin zaman serisine kaydetmektir.
- **Formül sabitleri:** z=10°C, T_ref=121.1°C (Bigelow/Ball formülü, standart, değiştirilemez — kullanıcı girdisi değildir).
- **LSL kaynağı:** FDA 21 CFR 113 (Thermally Processed Low-Acid Foods), minimum F₀ = 3.0 dakika (12D konsepti, D₁₂₁.₁≈0.21 dk *C. botulinum* için). Sektör pratiğinde genellikle 6-8 dk hedeflenir — bu ek bilgi UI'da gösterilir ama LSL olarak 3.0 kullanılır, tek bir "doğru" hedef değer gibi sunulmaz.
- **Method Validation kuralı:** formül, worked example ile `validation/process/` altına eklenip test edilmeden release edilmez.
- **Mevcut kod SIFIR regresyon:** `spc_core.py`, `compute_cpk()`, `render_bridge_widget()`'in kendisine (Faz 1/2'den) hiçbir değişiklik yapılmaz — sadece yeni bir formül + yeni bir panel + yeni bir bağımsız config eklenir.

---

### Task 1: `qc_converters.py` — Termal Letalite (F₀) formülü

**Files:**
- Modify: `src/qc_converters.py`
- Test: `tests/test_qc_converters.py`

**Interfaces:**
- Consumes: yok
- Produces: `thermal_lethality_f0(temperatures_c: list[float], delta_t_minutes: float, reference_temp_c: float = 121.1, z_value: float = 10.0) -> dict` — döner `{"f0_minutes": float}`. Formül: `F0 = delta_t_minutes * sum(10 ** ((T_i - reference_temp_c) / z_value) for T_i in temperatures_c)`.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_qc_converters.py icine eklenir

def test_thermal_lethality_f0_worked_example():
    # Bigelow/Ball formulu: F0 = dt * sum(10^((T_i - Tref)/z))
    # Tref=121.1, z=10.0 (sabit), dt=1.0 dk, 7 okumali bir come-up/hold/
    # come-down profili: 100.0, 115.0, 121.1, 121.1, 121.1, 115.0, 100.0
    # Elle hesaplama:
    #   100.0 -> 10^((100.0-121.1)/10) = 10^-2.11 = 0.0077624711...
    #   115.0 -> 10^((115.0-121.1)/10) = 10^-0.61  = 0.2454708915...
    #   121.1 -> 10^0 = 1.0 (x3)
    #   Toplam = 0.0077624711 + 0.2454708915 + 1 + 1 + 1 + 0.2454708915 + 0.0077624711
    #          = 3.5064667254...
    #   F0 = 1.0 * 3.5064667254... = 3.5064667254...
    result = thermal_lethality_f0(
        temperatures_c=[100.0, 115.0, 121.1, 121.1, 121.1, 115.0, 100.0],
        delta_t_minutes=1.0,
    )
    assert result["f0_minutes"] == pytest.approx(3.5065, abs=0.001)


def test_thermal_lethality_f0_at_exact_reference_temp():
    # Tum okumalar tam Tref'te ise her terim 10^0=1, toplam=n, F0=dt*n
    result = thermal_lethality_f0(
        temperatures_c=[121.1, 121.1, 121.1, 121.1], delta_t_minutes=0.5,
    )
    assert result["f0_minutes"] == pytest.approx(2.0, abs=0.0001)  # 0.5 * 4


def test_thermal_lethality_f0_empty_temperatures_raises():
    with pytest.raises(ValueError, match="sicaklik okumasi"):
        thermal_lethality_f0(temperatures_c=[], delta_t_minutes=1.0)


def test_thermal_lethality_f0_non_positive_delta_t_raises():
    with pytest.raises(ValueError, match="delta_t"):
        thermal_lethality_f0(temperatures_c=[121.1, 121.1], delta_t_minutes=0.0)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k thermal_lethality_f0`
Expected: FAIL with `NameError`/`ImportError` — fonksiyon henüz yok.

- [ ] **Step 3: Write implementation**

`src/qc_converters.py`'nin sonuna ekle:

```python
def thermal_lethality_f0(
    temperatures_c: list[float],
    delta_t_minutes: float,
    reference_temp_c: float = 121.1,
    z_value: float = 10.0,
) -> dict:
    """Bigelow/Ball formulu ile termal letalite (F0) hesabi.

    F0 = delta_t * sum(10 ** ((T_i - reference_temp_c) / z_value))

    temperatures_c: retort/proses sirasinda esit araliklarla okunan
        sicaklik degerleri listesi (C), zaman sirasina gore.
    delta_t_minutes: ardisik okumalar arasindaki sabit zaman araligi (dk).
    reference_temp_c: referans sicaklik (varsayilan 121.1C = 250F, standart).
    z_value: z-degeri (varsayilan 10C, standart - Clostridium botulinum
        icin termal direnc egrisinin egimi).

    Kaynak (formul): Bigelow (1921) / Ball (1923) genel letalite formulu -
    gida muhendisligi ders kitabi matematigi, ICUMSA Brix tablosu gibi
    erisilemez bir kaynak DEGIL.
    """
    if not temperatures_c:
        raise ValueError("en az bir sicaklik okumasi gereklidir")
    if delta_t_minutes <= 0:
        raise ValueError("delta_t_minutes sifir veya negatif olamaz")

    lethality_sum = sum(10 ** ((t - reference_temp_c) / z_value) for t in temperatures_c)
    return {"f0_minutes": delta_t_minutes * lethality_sum}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k thermal_lethality_f0`
Expected: 4 passed

- [ ] **Step 5: Run full suite (regression check)**

Run: `cd tests && python -m pytest -q`
Expected: tüm mevcut testler + 4 yeni test PASS, 0 FAIL.

- [ ] **Step 6: Commit**

```bash
git add src/qc_converters.py tests/test_qc_converters.py
git commit -m "feat: termal letalite (F0, Bigelow/Ball formulu) hesaplayicisi"
```

---

### Task 2: `constants.py` — F₀'ı Totox'la aynı desende minimal köprü parametresi olarak tanımla

**Files:**
- Modify: `src/constants.py`
- Test: `tests/test_qc_converters.py` (config smoke testi)

**Interfaces:**
- Produces: `F0_BRIDGE_PARAMETER_CONFIG: dict` — `TOTOX_BRIDGE_PARAMETER_CONFIG` ile AYNI ana anahtarları taşır (`unit`, `decimal_places`, `one_sided`, `is_individual`, `default_lsl`, `category`, `method_source`) ama `products` sözlüğü YOKTUR. `PARAMETER_CONFIG`'e KATILMAZ.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_qc_converters.py icine eklenir
from constants import F0_BRIDGE_PARAMETER_CONFIG


def test_f0_bridge_parameter_config_shape():
    cfg = F0_BRIDGE_PARAMETER_CONFIG
    assert cfg["unit"] == "dakika"
    assert cfg["one_sided"] is True
    assert cfg["is_individual"] is True
    assert cfg["default_lsl"] == 3.0
    assert cfg["category"] == "Proses"
    assert "products" not in cfg  # Altin Kural: tam parametre-registry uyeligi YOK


def test_f0_bridge_parameter_config_not_merged_into_registry():
    from constants import PARAMETER_CONFIG
    assert "F0 (Termal Letalite)" not in PARAMETER_CONFIG
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k f0_bridge`
Expected: FAIL with `ImportError: cannot import name 'F0_BRIDGE_PARAMETER_CONFIG'`

- [ ] **Step 3: Write minimal implementation**

`src/constants.py`'nin sonuna (Totox köprü config'inden sonra) ekle:

```python
# --- F0 Koprusu: Minimal I-MR Parametresi (v1.7 Faz 3 - QC Veri Donusturuculer) ---
# Totox koprusu ile BIREBIR AYNI mimari desen: F0 tam bir FOOD_QUALITY
# parametresi DEGIL - urun bazli LSL/USL arastirmasi gerekmez, cunku F0
# zaten FDA 21 CFR 113'ten gelen TEK, evrensel bir LSL'e (3.0 dakika,
# 12D konsepti, D121.1~=0.21 dk C. botulinum icin) sahiptir. Koprunun
# amaci sadece ham F0 degerini, kullanicinin sectigi herhangi bir I-MR
# parametresinin zaman serisine kaydetmektir - mevcut Viskozite
# parametresine OZEL olarak baglanmaz (Faz 3 kapsam karari).
F0_BRIDGE_PARAMETER_CONFIG = {
    "unit": "dakika",
    "decimal_places": 2,
    "one_sided": True,
    "is_individual": True,
    "default_lsl": 3.0,
    "category": "Proses",
    "method_source": "FDA 21 CFR 113 (Thermally Processed Low-Acid Foods) - minimum F0=3.0 dk (12D konsepti)",
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k f0_bridge`
Expected: 2 passed

- [ ] **Step 5: Commit**

```bash
git add src/constants.py tests/test_qc_converters.py
git commit -m "feat: F0 koprusu icin minimal F0_BRIDGE_PARAMETER_CONFIG"
```

---

### Task 3: `app.py` — Termal Letalite (F₀) UI paneli (Totox'un izlediği köprü deseni)

**Files:**
- Modify: `src/app.py` (`tab_calc` bloğu içine, Tuz (Mohr Metodu) panelinin `st.divider()`'ından SONRA, Totox `with st.container(border=True, key="card-24"):` bloğundan ÖNCE)

**Interfaces:**
- Consumes: `qc_converters.thermal_lethality_f0(temperatures_c, delta_t_minutes)` (Task 1), `constants.F0_BRIDGE_PARAMETER_CONFIG` (Task 2), `render_bridge_widget(values_by_target: dict[str, float], source_label, widget_key_prefix, extra_note=None)` (Faz 1/2'den, DEĞİŞMEDİ — I-MR dalı kullanılacak, tek bir float değer geçilecek, liste DEĞİL), `PARAMETER_CONFIG` (Totox'takiyle aynı şekilde "tüm I-MR parametreleri" listesini üretmek için)

- [ ] **Step 1: Import satırlarını güncelle**

`app.py`'nin import bloğuna ekle — `qc_converters`'ın zaten çok-isimli bir `from qc_converters import (...)` bloğu varsa (önceki fazlardan kalma) oraya `thermal_lethality_f0` ekle; `constants`'ın zaten çok-isimli bir `from constants import (...)` bloğu varsa oraya `F0_BRIDGE_PARAMETER_CONFIG` ekle. Var olan blokları KONTROL ET, tekrar eden yeni bir import satırı OLUŞTURMA.

- [ ] **Step 2: UI panelini ekle**

Tuz (Mohr Metodu) panelinin `st.divider()`'ından hemen sonra, Totox container'ından önce:

```python
    st.markdown("### 🌡️ Termal Letalite (F₀)")
    st.caption(
        "Bigelow/Ball formülü: F₀ = Δt × Σ 10^((T-121.1)/10). Retort/proses "
        "sırasında eşit aralıklarla okunan sıcaklık değerlerinden hesaplar. "
        f"Kaynak (LSL): {F0_BRIDGE_PARAMETER_CONFIG['method_source']} — "
        "sektör pratiğinde genellikle ek güvenlik payı için 6-8 dk hedeflenir "
        "(bu tek bir 'doğru' hedef değil, LSL=3.0 dk resmi minimumdur)."
    )

    col_f0_1, col_f0_2 = st.columns(2)
    with col_f0_1:
        f0_delta_t = st.number_input(
            "Okumalar arası zaman aralığı Δt (dk)", min_value=0.0001, value=1.0,
            step=0.1, key="qc_f0_delta_t",
        )
    with col_f0_2:
        f0_reading_count = st.number_input(
            "Sıcaklık okuma sayısı", min_value=2, max_value=60, value=7,
            step=1, key="qc_f0_reading_count",
        )

    st.caption(f"{f0_reading_count} adet sıcaklık okuması girin (zaman sırasına göre):")
    f0_temps = []
    f0_cols = st.columns(min(f0_reading_count, 10))
    for _f0_i in range(f0_reading_count):
        with f0_cols[_f0_i % len(f0_cols)]:
            _f0_t = st.number_input(
                f"T{_f0_i + 1} (°C)", min_value=0.0, max_value=200.0, value=121.1,
                step=0.1, key=f"qc_f0_temp_{_f0_i}",
            )
            f0_temps.append(_f0_t)

    try:
        f0_result = thermal_lethality_f0(temperatures_c=f0_temps, delta_t_minutes=f0_delta_t)
        f0_value = f0_result["f0_minutes"]
        st.metric("Hesaplanan F₀ (dk)", f"{f0_value:.2f}")
        f0_lsl = F0_BRIDGE_PARAMETER_CONFIG["default_lsl"]
        if f0_value < f0_lsl:
            st.warning(f"F₀={f0_value:.2f} dk, FDA minimum LSL'in ({f0_lsl} dk) ALTINDA.")
        else:
            st.success(f"F₀={f0_value:.2f} dk, FDA minimum LSL'i ({f0_lsl} dk) karşılıyor.")

        # Totox koprusuyle AYNI mimari desen: F0'in kendi ayri bir
        # PARAMETER_CONFIG kaydi olmadigi icin (bkz. F0_BRIDGE_PARAMETER_CONFIG
        # notu, constants.py) hedef, uygulamadaki TUM I-MR parametreleri
        # arasindan acikca secilir - mevcut Viskozite parametresine OZEL
        # olarak baglanmaz (Faz 3 kapsam karari).
        _f0_individual_params = sorted(
            p for p, cfg in PARAMETER_CONFIG.items() if cfg.get("is_individual", False)
        )
        render_bridge_widget(
            values_by_target=dict.fromkeys(_f0_individual_params, f0_value),
            source_label="Termal Letalite (F₀)",
            widget_key_prefix="qc_f0",
            extra_note=(
                "Not: bu köprü sadece I-MR zaman serisine ham değer kaydeder; "
                "grafik/Cpk hesaplaması seçilen parametrenin kendi LSL/USL "
                "spesifikasyonunu kullanır, F₀'ın FDA referans alt sınırı "
                f"({F0_BRIDGE_PARAMETER_CONFIG['default_lsl']} dk) sadece bilgi "
                "amaçlıdır."
            ),
        )
    except ValueError as exc:
        st.error(f"Girdi hatası: {exc}")

    st.divider()
```

- [ ] **Step 3: Syntax kontrolü + tam regresyon**

Run: `cd src && python -c "import ast; ast.parse(open('app.py', encoding='utf-8').read())"`
Expected: hata yok.

Run: `cd tests && python -m pytest -q`
Expected: mevcut tüm testler geçer, sıfır regresyon.

- [ ] **Step 4: Widget key benzersizliği kontrolü**

`grep -n "qc_f0_" src/app.py` çalıştırıp bu panelin ürettiği anahtarların (`qc_f0_delta_t`, `qc_f0_reading_count`, `qc_f0_temp_{i}`, ve `render_bridge_widget`'ın ürettiği `qc_f0_target_param`/`qc_f0_bridge_button`) dosyanın başka hiçbir yerinde (özellikle `qc_ta_*`, `qc_salt_*`, `qc_moisture_*`, `totox_*` önekleriyle) çakışmadığını doğrula.

- [ ] **Step 5: Manuel doğrulama**

Run: `cd src && streamlit run app.py`

Tarayıcıda: "Hızlı Hesaplayıcılar" sekmesine git → Termal Letalite paneli görünmeli → Δt=1.0, okuma sayısı=7 → 7 sıcaklık kutusuna [100.0, 115.0, 121.1, 121.1, 121.1, 115.0, 100.0] gir → F₀≈3.51 dk görünmeli, LSL uyarı/başarı mesajı doğru olmalı → hedef parametre dropdown'undan bir I-MR parametresi (örn. Viskozite) seç → aktif parametreyi de Viskozite yap → "SPC Veri Setine Aktar" butonuna bas → başarı mesajı, ekstra not F₀'ın kendi LSL'inin sadece bilgi amaçlı olduğunu belirtmeli.

- [ ] **Step 6: Commit**

```bash
git add src/app.py
git commit -m "feat: Termal Letalite (F0) UI paneli (Totox koprusu deseni)"
```

---

### Task 4: `validation/process/` — F₀ formülü için Method Validation

**Files:**
- Modify: `validation/process/titration_reference.csv` (YENİ satır eklenir — dosya adı yanıltıcı olsa da, `validation/process/README.md`'nin genel çerçevesi "ölçüm standardizasyon formülü doğrulaması" olduğu için bu dosyayı genişletmek yerine F₀'a özgü yeni bir dosya açılır, bkz. Step 1)
- Create: `validation/process/thermal_lethality_reference.csv`
- Modify: `validation/process/README.md`
- Modify: `tests/test_validation_suite.py`

**Interfaces:**
- Consumes: `qc_converters.thermal_lethality_f0`

- [ ] **Step 1: `validation/process/thermal_lethality_reference.csv` oluştur**

```csv
source,temperatures_c,delta_t_minutes,expected_f0_minutes,tolerance,notes
"Bigelow/Ball formulu elle hesaplama: F0 = dt * sum(10^((T-121.1)/10)), z=10, Tref=121.1 (standart sabitler)","100.0;115.0;121.1;121.1;121.1;115.0;100.0",1.0,3.5065,0.001,"Come-up/hold/come-down profili - dahili matematiksel dogrulama, dis kaynak DEGIL (formul ders kitabi matematigi, ICUMSA gibi erisilemez bir kaynak degil)"
```

- [ ] **Step 2: `validation/process/README.md`'yi güncelle**

Mevcut dosyanın sonuna ekle:

```markdown

## thermal_lethality_reference.csv (v1.7 Faz 3)

`thermal_lethality_f0()`'ı doğrudan doğrular (Bigelow/Ball formülü). Formülün
kendisi ders kitabı matematiğidir (z=10°C, T_ref=121.1°C standart sabitler) —
`titration_reference.csv`'deki meq faktörleri gibi türetilmiş bir katsayı
DEĞİL, doğrudan uygulanan bir formül. `temperatures_c` sütunu `;` ile
ayrılmış bir sıcaklık listesi taşır.
```

- [ ] **Step 3: `test_validation_suite.py`'ye yeni test fonksiyonu ekle**

Dosyanın import bloğuna ekle (mevcut importların yanına):

```python
from qc_converters import thermal_lethality_f0
```

Dosyanın sonuna ekle:

```python
def test_process_thermal_lethality_reference_csv_matches_formula():
    df = _load("thermal_lethality_reference.csv", subdir="process")
    for _, row in df.iterrows():
        temps = [float(t) for t in str(row["temperatures_c"]).split(";")]
        result = thermal_lethality_f0(
            temperatures_c=temps, delta_t_minutes=row["delta_t_minutes"],
        )
        actual = result["f0_minutes"]
        assert abs(actual - row["expected_f0_minutes"]) <= row["tolerance"], (
            f"thermal_lethality_f0 uyusmuyor: hesaplanan={actual}, "
            f"beklenen={row['expected_f0_minutes']} (kaynak: {row['source']})"
        )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_validation_suite.py -v -k thermal_lethality`
Expected: 1 passed

- [ ] **Step 5: Tüm test paketini çalıştır (regresyon kontrolü)**

Run: `cd tests && python -m pytest -q`
Expected: tüm mevcut testler + bu fazın yeni testleri PASS, 0 FAIL.

- [ ] **Step 6: Commit**

```bash
git add validation/process/thermal_lethality_reference.csv validation/process/README.md tests/test_validation_suite.py
git commit -m "test: validation/process/thermal_lethality_reference.csv - F0 formul dogrulamasi"
```

---

### Task 5: `METHODOLOGY.md` — Faz 3'ü tamamlandı olarak işaretle

**Files:**
- Modify: `METHODOLOGY.md`

- [ ] **Step 1: "Faz 3 — Karmaşık Validasyon Gerektirenler (sadece F₀)" başlığını güncelle**

`METHODOLOGY.md`'deki `**Faz 3 — Karmaşık Validasyon Gerektirenler (sadece F₀)**` başlığını `**Faz 3 — Karmaşık Validasyon Gerektirenler (sadece F₀) ✅ Tamamlandı**` yap ve aşağıya şu implementasyon notunu ekle:

```markdown

Implementasyon: `src/qc_converters.py` (`thermal_lethality_f0()` — Bigelow/
Ball formülü), `src/constants.py` (`F0_BRIDGE_PARAMETER_CONFIG` — Totox
köprüsüyle birebir aynı mimari desen, `PARAMETER_CONFIG`'e katılmaz),
`src/app.py` "Hızlı Hesaplayıcılar" sekmesi (yeni panel, herhangi bir
I-MR parametresine köprülenebilir — mevcut Viskozite parametresine özel
DEĞİL), `validation/process/thermal_lethality_reference.csv`.

**v1.7 QC Veri Dönüştürücüler roadmap'i bu fazla TAMAMLANMIŞTIR** (Faz
1: Köprü Altyapısı + Gravimetrik Nem + Totox köprüsü; Faz 2: Titrimetrik
Dönüştürücüler + köprünün X-bar/R'a genişletilmesi; Faz 3: Termal
Letalite). Brix Sıcaklık Düzeltmesi (Faz 1'de kaynak erişimi
bekleniyor olarak bırakılmıştı) ve Bostwick/Viskozite Sıcaklık
Normalizasyonu (Faz 3 planlamasında kalıcı olarak kapsam dışı
bırakıldı) hâlâ askıda/hariç — bkz. ilgili notlar yukarıda.
```

- [ ] **Step 2: Commit**

```bash
git add METHODOLOGY.md
git commit -m "docs: v1.7 Faz 3'u (F0) tamamlandi olarak isaretle"
```

---

## Self-Review Notu (plan yazarı için)

- **Spec coverage:** METHODOLOGY.md'deki Faz 3'ün tek maddesi (F₀) → Task 1 (formül), Task 2 (köprü config), Task 3 (UI paneli), Task 4 (validation). Docs → Task 5. Hepsi kapsandı. Bostwick kasıtlı olarak kapsam dışı bırakıldığı için hiçbir task ona değinmiyor.
- **Totox deseni tutarlılığı:** F0_BRIDGE_PARAMETER_CONFIG, TOTOX_BRIDGE_PARAMETER_CONFIG ile AYNI anahtar setini taşıyor (`unit`, `decimal_places`, `one_sided`, `is_individual`, `category`, `method_source` — Totox'ta `default_usl`, F0'da `default_lsl`, yön farkı formülün doğasından, iki tarafın da tek bir evrensel sabiti var). Task 3'teki UI paneli, Totox'un `_totox_individual_params` desenini `_f0_individual_params` olarak birebir tekrarlıyor — kod tekrarı kabul edilebilir (Faz 1/2'de zaten kurulan bir desen, merkezi bir yardımcıya çıkarmak bu planın kapsamı dışında, gelecekte 3. bir "herhangi I-MR'a bağlanabilir" dönüştürücü gelirse değerlendirilebilir).
- **Tip tutarlılığı:** `render_bridge_widget`'a I-MR dalı için tek bir `float` değer geçiliyor (liste DEĞİL) — Faz 2'nin X-bar/R değişikliklerinden ETKİLENMİYOR, mevcut I-MR dalı hiç değişmedi.
- **compute_cpk() kısıtı:** F0'ın LSL-only doğası (yüksek F0 = güvenli), mevcut `compute_cpk()`'ın `one_sided=True` modunun HER ZAMAN Cpu (sadece USL) hesapladığı gerçeğiyle çelişiyor — ama bu Faz 3'ün kapsamı DIŞINDA: F0 kendi başına chart edilebilir bir PARAMETER_CONFIG üyesi DEĞİL (Totox gibi), sadece MEVCUT bir I-MR parametresinin (kendi LSL/USL'iyle) verisine köprüleniyor. Dolayısıyla F0_BRIDGE_PARAMETER_CONFIG'teki `one_sided`/`default_lsl` alanları hiçbir zaman `compute_cpk()`'a doğrudan geçmez — sadece bilgilendirme mesajında kullanılır. `spc_core.py`'ye dokunmadan bu kısıt aşılmış olur.

---

⬅ [METHODOLOGY.md'ye dön](../../../METHODOLOGY.md)
