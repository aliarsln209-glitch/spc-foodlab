# v1.7 Faz 1 — QC Veri Dönüştürücüler: SPC Entegrasyon Köprüsü Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SPC FoodLab'e, mevcut SPC/Cpk motoruna dokunmadan, "QC Veri Dönüştürücü" panelleri (Gravimetrik Nem/Kuru Madde, Totox köprüsü — Brix ayrı bir bloğa taşındı, bkz. aşağıda) eklemek — her biri "SPC Veri Setine Aktar" butonuyla `st.session_state.subgroups`'a bağlanabilir.

**Architecture:** Yeni, Streamlit'ten bağımsız bir `src/qc_converters.py` pure-logic modülü (mikrobiology.py ile aynı mimari ilke) formülleri ve köprü giriş noktasını taşır. `src/app.py`'deki "Hızlı Hesaplayıcılar" sekmesi (SEKME 3), Totox'un hemen yanına yeni dönüştürücü panelleri ekler; her panelin "SPC Veri Setine Aktar" butonu `st.session_state.subgroups.append(build_bridge_subgroup_entry(...))` çağırır — mevcut chart/Cpk kodu SIFIR değişiklik görür çünkü `subgroups` listesi zaten `{"shift": str, "values": list[float]}` şemasını bekliyor.

**Tech Stack:** Python, Streamlit, pytest, pandas (validation CSV okuma) — mevcut stack, yeni bağımlılık yok.

**Spec:** `METHODOLOGY.md` → "v1.7 — QC Veri Dönüştürücüler (fazlı)" bölümü, "Faz 1 — Köprü Altyapısı + 2 modül + Totox bağlantısı" alt maddesi.

## Kapsam notu — Brix ayrıldı

Brix Sıcaklık Düzeltmesi (ICUMSA), kaynağı (AOAC 932.14c veya USDA AMS resmi
tablosu) bu oturumda doğrulanamadığı için (WebFetch 403 — paywall/bot
engelleme, kaynağın kalitesizliği değil) Faz 1'in ana akışından **ayrıldı**.
Gerekçe: projenin "kaynak doğrulanamazsa sayı uydurulmaz" disiplini gereği,
tek bir doğrulanmamış katsayı tablosu yüzünden Köprü + Nem + Totox gibi
bağımsız, düşük riskli teslimatları bekletmek anlamsız. Brix, **Task 8-9**'da
ayrı, "BLOCKED — kaynak erişimi bekleniyor" olarak işaretli bir blokta durur;
kaynağa erişildiğinde (kütüphane aboneliği veya fiziksel kütüphane) o iki
task bağımsız olarak tamamlanabilir, Task 1-7'nin tekrar açılmasını
gerektirmez.

## Global Constraints

- **Altın Kural:** Her yeni dönüştürücü, çıktısını SPC kontrol kartının Y eksenine (mevcut bir parametrenin `subgroups` verisine) bağlamalıdır — bağımsız bir "araç sayfası" değil, veri giriş kapısıdır.
- **Method Validation kuralı (v1.2'den beri zorunlu):** Her yeni formül, literatürden worked example ile `validation/process/` altına referans dosyası eklenip `test_validation_suite.py` üzerinden doğrulanmadan release edilmez.
- **No-fabrication disiplini:** Kaynağı doğrulanamayan hiçbir sayı/katsayı uydurulmaz — Hammadde Kütüphanesi'ndeki `{"range": None, "source": None, "verified": False, "note": None}` deseni birebir tekrar kullanılır.
- **Totox'un statüsü:** Tam bir `FOOD_QUALITY_PARAMETER_CONFIG` üyesi (ürün bazlı `products` sözlüğü, LSL/USL kaynak araştırması) DEĞİLDİR — sadece ham Totox değerini I-MR zaman serisine besleyen, sabit/evrensel bir USL (`TOTOX_LIMIT = 26.0`, zaten `app.py`'de mevcut) taşıyan minimal bir kayıt.
- **Mevcut kod SIFIR regresyon:** `spc_core.py`, `compute_cpk()`, X-bar/R ve I-MR motorlarına bu fazda hiçbir değişiklik yapılmaz — sadece `subgroups` listesine yeni satır eklenir, format aynı kalır.

---

### Task 1: `qc_converters.py` — Gravimetrik Nem/Kuru Madde hesabı

En düşük riskli modülden başlanır: standart AOAC 925.10 gravimetrik yöntemi, dış kaynak araştırması gerektirmeyen saf aritmetik.

**Files:**
- Create: `src/qc_converters.py`
- Test: `tests/test_qc_converters.py`

**Interfaces:**
- Produces: `gravimetric_moisture(dish_tare_g: float, wet_with_dish_g: float, dry_with_dish_g: float) -> dict` — döner: `{"wet_sample_g": float, "dry_sample_g": float, "moisture_pct": float, "dry_matter_pct": float}`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_qc_converters.py
import math
import os
import sys

import pytest

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

from qc_converters import gravimetric_moisture


def test_gravimetric_moisture_basic_worked_example():
    # AOAC 925.10 gravimetrik nem tayini deseni: dara + yas + kuru agirlik
    # Tare (bos kap) = 25.000 g, Yas (kap+numune) = 30.000 g (net 5.000 g numune)
    # Kuru (kap+kalinti) = 29.400 g (net 4.400 g kuru kalinti)
    # Nem kaybi = 5.000 - 4.400 = 0.600 g -> %Nem = 0.600/5.000*100 = 12.0
    result = gravimetric_moisture(
        dish_tare_g=25.000, wet_with_dish_g=30.000, dry_with_dish_g=29.400
    )
    assert result["wet_sample_g"] == pytest.approx(5.000, abs=0.001)
    assert result["dry_sample_g"] == pytest.approx(4.400, abs=0.001)
    assert result["moisture_pct"] == pytest.approx(12.0, abs=0.01)
    assert result["dry_matter_pct"] == pytest.approx(88.0, abs=0.01)


def test_gravimetric_moisture_zero_wet_sample_raises():
    with pytest.raises(ValueError, match="net yas numune agirligi"):
        gravimetric_moisture(dish_tare_g=25.000, wet_with_dish_g=25.000, dry_with_dish_g=25.000)


def test_gravimetric_moisture_dry_heavier_than_wet_raises():
    # Kuru kalinti net agirligi, yas numune net agirligindan buyuk olamaz
    with pytest.raises(ValueError, match="kuru kalinti"):
        gravimetric_moisture(dish_tare_g=25.000, wet_with_dish_g=30.000, dry_with_dish_g=30.500)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd tests && python -m pytest test_qc_converters.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'qc_converters'`

- [ ] **Step 3: Write minimal implementation**

```python
# src/qc_converters.py
"""QC Veri Donusturucu formulleri - Streamlit'ten bagimsiz, saf mantik.

Mimari ilke microbiology.py ile aynidir: hicbir fonksiyon st.session_state'e
dokunmaz, sadece deger hesaplar/dondurur. session_state entegrasyonu app.py'de
build_bridge_subgroup_entry() ciktisini subgroups listesine ekleyerek yapilir.
"""


def gravimetric_moisture(
    dish_tare_g: float, wet_with_dish_g: float, dry_with_dish_g: float
) -> dict:
    """AOAC 925.10 gravimetrik nem/kuru madde tayini.

    dish_tare_g: bos kabin agirligi (g)
    wet_with_dish_g: kap + yas numune agirligi (g)
    dry_with_dish_g: kap + kurutma sonrasi kalinti agirligi (g)
    """
    wet_sample_g = wet_with_dish_g - dish_tare_g
    dry_sample_g = dry_with_dish_g - dish_tare_g

    if wet_sample_g <= 0:
        raise ValueError("net yas numune agirligi sifir veya negatif olamaz")
    if dry_sample_g > wet_sample_g:
        raise ValueError("kuru kalinti agirligi, yas numune agirligindan buyuk olamaz")

    moisture_pct = (wet_sample_g - dry_sample_g) / wet_sample_g * 100.0
    dry_matter_pct = 100.0 - moisture_pct

    return {
        "wet_sample_g": wet_sample_g,
        "dry_sample_g": dry_sample_g,
        "moisture_pct": moisture_pct,
        "dry_matter_pct": dry_matter_pct,
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_qc_converters.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add src/qc_converters.py tests/test_qc_converters.py
git commit -m "feat: gravimetrik nem/kuru madde hesaplayicisi (qc_converters.py)"
```

---

### Task 2: `qc_converters.py` — SPC Entegrasyon Köprüsü (`build_bridge_subgroup_entry`)

Faz 1'in paylaşılan altyapısı — Nem/Kuru Madde ve Totox köprüleri bunu kullanır (Brix köprüsü de kaynağı doğrulandığında AYNI fonksiyonu kullanacak, bkz. Task 9).

**Files:**
- Modify: `src/qc_converters.py`
- Test: `tests/test_qc_converters.py`

**Interfaces:**
- Consumes: yok (saf fonksiyon)
- Produces: `build_bridge_subgroup_entry(value: float, shift_label: str) -> dict` — döner `{"shift": shift_label, "values": [value]}`. Bu, `app.py:96-97`'deki `st.session_state.subgroups` listesinin BEKLEDİĞİ `{"shift": str, "values": list[float]}` şemasıyla birebir uyumludur — sonraki task'lar `st.session_state.subgroups.append(build_bridge_subgroup_entry(...))` şeklinde çağırır.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_qc_converters.py icine eklenir

from qc_converters import build_bridge_subgroup_entry


def test_build_bridge_subgroup_entry_shape():
    entry = build_bridge_subgroup_entry(value=12.34, shift_label="QC Donusturucu - Test")
    assert entry == {"shift": "QC Donusturucu - Test", "values": [12.34]}


def test_build_bridge_subgroup_entry_rejects_non_finite_value():
    with pytest.raises(ValueError, match="sonlu"):
        build_bridge_subgroup_entry(value=float("nan"), shift_label="QC Donusturucu - Test")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k bridge`
Expected: FAIL with `ImportError: cannot import name 'build_bridge_subgroup_entry'`

- [ ] **Step 3: Write minimal implementation**

```python
# src/qc_converters.py sonuna eklenir

import math


def build_bridge_subgroup_entry(value: float, shift_label: str) -> dict:
    """QC donusturucu sonucunu, mevcut subgroups sema formatina cevirir.

    app.py:96-97'deki st.session_state.subgroups listesi
    {"shift": str, "values": list[float]} sekli bekler; koprubu format
    degistirmez, sadece dogru sekli uretir - spc_core.py'ye HICBIR
    degisiklik gerekmez.
    """
    if not math.isfinite(value):
        raise ValueError("kopru degeri sonlu bir sayi olmalidir (NaN/inf kabul edilmez)")
    return {"shift": shift_label, "values": [value]}
```

(`import math` dosyanın en üstüne taşınır, `gravimetric_moisture` fonksiyonundan önce.)

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_qc_converters.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add src/qc_converters.py tests/test_qc_converters.py
git commit -m "feat: SPC entegrasyon koprusu build_bridge_subgroup_entry()"
```

---

### Task 3: `constants.py` — Totox'u minimal I-MR köprü parametresi olarak tanımla

**Files:**
- Modify: `src/constants.py`
- Test: `tests/test_qc_converters.py` (config smoke testi)

**Interfaces:**
- Produces: `TOTOX_BRIDGE_PARAMETER_CONFIG: dict` — `FOOD_QUALITY_PARAMETER_CONFIG` ile AYNI ana anahtarları taşır (`unit`, `decimal_places`, `one_sided`, `is_individual`, `category`, `method_source`) ama `products` sözlüğü YOKTUR — çünkü USL evrenseldir, ürün bazlı değişmez.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_qc_converters.py icine eklenir
from constants import TOTOX_BRIDGE_PARAMETER_CONFIG


def test_totox_bridge_parameter_config_shape():
    cfg = TOTOX_BRIDGE_PARAMETER_CONFIG
    assert cfg["unit"] == "meq O2/kg"
    assert cfg["one_sided"] is True
    assert cfg["is_individual"] is True
    assert cfg["default_usl"] == 26.0
    assert cfg["category"] == "Proses"
    assert "products" not in cfg  # Altin Kural: tam parametre-registry uyeligi YOK
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k totox_bridge`
Expected: FAIL with `ImportError: cannot import name 'TOTOX_BRIDGE_PARAMETER_CONFIG'`

- [ ] **Step 3: Write minimal implementation**

```python
# src/constants.py icine eklenir (FOOD_QUALITY_PARAMETER_CONFIG tanimindan
# SONRA, ayri bir sabit olarak - Totox bu sozluge KATILMAZ, bkz. METHODOLOGY.md
# "v1.7 - QC Veri Donusturuculer" Faz 1 notu)

# Totox koprusu: tam bir FOOD_QUALITY parametresi DEGIL - urun bazli LSL/USL
# arastirmasi gerektirmez, cunku Totox = 2*PV + AnV zaten Codex/IOC'ten gelen
# TEK, evrensel bir USL'e (TOTOX_LIMIT = 26.0, app.py) sahiptir. Koprunun
# amaci sadece ham degeri I-MR zaman serisine kaydetmektir.
TOTOX_BRIDGE_PARAMETER_CONFIG = {
    "unit": "meq O2/kg",
    "decimal_places": 2,
    "one_sided": True,
    "is_individual": True,
    "default_usl": 26.0,
    "category": "Proses",
    "method_source": "Codex Alimentarius / IOC (International Olive Council) - Totox = 2xPV + AnV birlesik indeks",
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k totox_bridge`
Expected: 1 passed

- [ ] **Step 5: Commit**

```bash
git add src/constants.py tests/test_qc_converters.py
git commit -m "feat: Totox koprusu icin minimal TOTOX_BRIDGE_PARAMETER_CONFIG"
```

---

### Task 4: `app.py` — "SPC Veri Setine Aktar" UI: Gravimetrik Nem/Kuru Madde paneli

**Files:**
- Modify: `src/app.py` (SEKME 3 "Hızlı Hesaplayıcılar", Totox bölümünden ÖNCE — yeni bir alt-bölüm olarak, `tab_calc` bloğunun başı, satır ~2745 civarı)

**Interfaces:**
- Consumes: `qc_converters.gravimetric_moisture(dish_tare_g, wet_with_dish_g, dry_with_dish_g)`, `qc_converters.build_bridge_subgroup_entry(value, shift_label)`, mevcut `st.session_state.subgroups` listesi (`app.py:96-97`)

- [ ] **Step 1: UI panelini ekle**

`app.py`'nin en üstündeki import bloğuna ekle:

```python
from qc_converters import build_bridge_subgroup_entry, gravimetric_moisture
```

`tab_calc` bloğunun başına (Totox bölümünden ÖNCE):

```python
with tab_calc:
    st.markdown("### ⚖️ Gravimetrik Nem / Kuru Madde")
    st.caption("AOAC 925.10 yöntemi: dara + yaş numune + kuru kalıntı ağırlığından hesaplar.")

    col_g1, col_g2, col_g3 = st.columns(3)
    with col_g1:
        dish_tare = st.number_input("Kap darası (g)", min_value=0.0, value=25.000, step=0.001, format="%.3f", key="qc_moisture_tare")
    with col_g2:
        wet_with_dish = st.number_input("Kap + yaş numune (g)", min_value=0.0, value=30.000, step=0.001, format="%.3f", key="qc_moisture_wet")
    with col_g3:
        dry_with_dish = st.number_input("Kap + kuru kalıntı (g)", min_value=0.0, value=29.400, step=0.001, format="%.3f", key="qc_moisture_dry")

    try:
        moisture_result = gravimetric_moisture(dish_tare, wet_with_dish, dry_with_dish)
        col_r1, col_r2 = st.columns(2)
        col_r1.metric("Nem (%)", f"{moisture_result['moisture_pct']:.2f}")
        col_r2.metric("Kuru Madde (%)", f"{moisture_result['dry_matter_pct']:.2f}")

        moisture_target = st.selectbox(
            "Hangi SPC parametresine aktarılsın?",
            ["Nem/Rutubet", "Kuru Madde"],
            key="qc_moisture_target_param",
        )
        if st.button("📌 SPC Veri Setine Aktar (Nem/Kuru Madde)", key="qc_moisture_bridge_button"):
            value = (
                moisture_result["moisture_pct"]
                if moisture_target == "Nem/Rutubet"
                else moisture_result["dry_matter_pct"]
            )
            entry = build_bridge_subgroup_entry(
                value=value, shift_label=f"QC Dönüştürücü - Gravimetrik {moisture_target}",
            )
            st.session_state.subgroups.append(entry)
            st.success(
                f"{moisture_target} değeri SPC veri setine eklendi. "
                f"Chart & Cpk sekmesinde Parametre olarak '{moisture_target}' seçili olduğundan emin olun."
            )
    except ValueError as exc:
        st.error(f"Girdi hatası: {exc}")

    st.divider()
```

- [ ] **Step 2: Manuel doğrulama**

Run: `cd src && streamlit run app.py`

Tarayıcıda: Dara=25.000, Yaş=30.000, Kuru=29.400 gir → Nem=%12.00, Kuru Madde=%88.00 görünmeli → "Nem/Rutubet" seç → aktar butonuna bas → başarı mesajı → geçersiz girdi (Kuru > Yaş) dene → `st.error` mesajı görünmeli (uygulama çökmemeli) → "Veri Girişi" sekmesine dön, Parametre="Nem/Rutubet" seçiliyse yeni satırın `st.session_state.subgroups`'a eklendiğini (ham veri tablosunda) doğrula.

- [ ] **Step 3: Commit**

```bash
git add src/app.py
git commit -m "feat: Gravimetrik Nem/Kuru Madde UI paneli + SPC koprusu"
```

---

### Task 5: `app.py` — Totox köprü butonu (mevcut Totox bölümüne ekleme)

**Files:**
- Modify: `src/app.py` (mevcut Totox bölümü, `st.session_state.totox_history.append(...)` çağrısından hemen sonra, ~satır 2894)

**Interfaces:**
- Consumes: `constants.TOTOX_BRIDGE_PARAMETER_CONFIG`, `qc_converters.build_bridge_subgroup_entry(...)`, mevcut `totox_value` değişkeni (zaten `app.py:2827`'de hesaplı)

- [ ] **Step 1: Mevcut "Sonucu geçmişe ekle" butonunun yanına köprü butonu ekle**

`app.py`'nin en üstündeki import bloğuna ekle:

```python
from constants import TOTOX_BRIDGE_PARAMETER_CONFIG
```

Mevcut kod (satır ~2876-2894), "NOT: Bu sekme ... session_state.subgroups'a hiçbir şekilde dokunmaz" yorumunun ALTINA şu not eklenir (yorum artık kısmen geçersiz, güncellenir):

```python
    # NOT (v1.7 Faz 1 guncellemesi): "SPC Veri Setine Aktar" butonu KASITLI
    # bir istisnadir - yukaridaki izolasyon yorumu hala gecerlidir ama artik
    # KULLANICI ACIKCA ISTERSE subgroups'a tek satir eklenebilir (otomatik
    # DEGIL). Totox tam bir FOOD_QUALITY parametresi degildir (bkz.
    # constants.TOTOX_BRIDGE_PARAMETER_CONFIG, urun bazli LSL/USL yok).
    col_hist1, col_hist2 = st.columns(2)
    with col_hist1:
        if st.button("📌 Sonucu geçmişe ekle", key="totox_add_history"):
            st.session_state.totox_history.append({
                "Zaman": datetime.now().strftime("%H:%M:%S"),
                "PV": round(totox_peroxide, 2), "AnV": round(totox_anisidine, 2),
                "Totox": round(totox_value, 2),
                "Durum": "Uygun" if totox_in_range else "Sınır dışı",
            })
    with col_hist2:
        if st.button("📌 SPC Veri Setine Aktar (Totox → I-MR)", key="totox_spc_bridge_button"):
            entry = build_bridge_subgroup_entry(
                value=totox_value, shift_label="QC Dönüştürücü - Totox",
            )
            st.session_state.subgroups.append(entry)
            st.success(
                "Totox değeri SPC veri setine eklendi. Bu, Totox'u tam bir SPC "
                "parametresi yapmaz — sadece I-MR zaman serisine ham değer kaydeder "
                f"(evrensel USL={TOTOX_BRIDGE_PARAMETER_CONFIG['default_usl']} meq O2/kg)."
            )
```

**Not:** Mevcut tek-buton kod (`if st.button("📌 Sonucu gecmise ekle", ...)`) iki-sütunlu bir düzene taşınır — mevcut buton davranışı DEĞİŞMEZ, sadece yanına ikinci buton eklenir.

- [ ] **Step 2: Manuel doğrulama**

Run: `cd src && streamlit run app.py`

Tarayıcıda: Totox sekmesine git → PV=8.0, AnV=4.0 gir (Totox=20.0) → "SPC Veri Setine Aktar" butonuna bas → başarı mesajı görünmeli → mevcut "Sonucu geçmişe ekle" butonunun HALA çalıştığını doğrula (regresyon kontrolü).

- [ ] **Step 3: Commit**

```bash
git add src/app.py
git commit -m "feat: Totox SPC koprusu - ham degeri I-MR zaman serisine ekleme"
```

---

### Task 6: `validation/process/` klasörü + Method Validation testleri

**Files:**
- Create: `validation/process/cpk_reference.csv`
- Create: `validation/process/README.md` (kısa, `validation/README.md`'deki genel açıklamaya link verir)
- Modify: `tests/test_validation_suite.py`

**Interfaces:**
- Consumes: `qc_converters.gravimetric_moisture`, mevcut `_load(filename, subdir)` ve `_assert_cpk_csv_matches_formula(df)` yardımcıları (`tests/test_validation_suite.py`)

- [ ] **Step 1: `validation/process/cpk_reference.csv` oluştur**

Mevcut `validation/physical/cpk_reference.csv` ile AYNI şema (`source,x_double_bar,r_bar,n,lsl,usl,one_sided,expected_cpk,tolerance,notes`) — bu dosya, Task 1'deki gravimetrik nem worked example'ının bir I-MR/Cpk senaryosuna dönüştürülmüş hali. Brix satırı, Task 9 (kaynak doğrulandığında) buraya EKLENECEK — şimdi eklenmez:

```csv
source,x_double_bar,r_bar,n,lsl,usl,one_sided,expected_cpk,tolerance,notes
"AOAC 925.10 gravimetrik nem - qc_converters.gravimetric_moisture worked example (12.0 nem)",12.0,0.0,2,10.0,14.0,False,0.333,0.01,"Dahili matematiksel tutarlilik kontrolu - urun spesifikasyonu DEGIL, olcum standardizasyon formulu dogrulamasi"
```

(Not: `r_bar=0.0` tek nokta için basitleştirilmiş bir dahili-tutarlılık satırıdır — `validation/optics/`'teki "dahili matematiksel tutarlılık kontrolü" desenini takip eder, ürün spesifikasyonu iddiası taşımaz.)

- [ ] **Step 2: `validation/process/README.md` oluştur**

```markdown
# validation/process/

Bu klasör, chemistry/physical/optics/microbiology'den AYRI bir kategoridir:
buradaki referanslar **ürün spesifikasyonu DEĞİL**, QC Veri Dönüştürücüler
(v1.7 Faz 1) için ölçüm standardizasyon formülü doğrulamasıdır (AOAC
gravimetrik nem; ICUMSA Brix düzeltmesi kaynak doğrulandığında buraya
eklenecek, bkz. plan Task 8-9; ileride Mohr/AOAC titrimetrik faktörler).

Detay: [METHODOLOGY.md](../../METHODOLOGY.md) → "v1.7 — QC Veri Dönüştürücüler".
```

- [ ] **Step 3: `test_validation_suite.py`'ye yeni domain testi ekle**

```python
# tests/test_validation_suite.py icine eklenir

def test_process_cpk_reference_csv_matches_formula():
    df = _load("cpk_reference.csv", subdir="process")
    _assert_cpk_csv_matches_formula(df)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_validation_suite.py -v -k process`
Expected: 1 passed

- [ ] **Step 5: Tüm test paketini çalıştır (regresyon kontrolü)**

Run: `cd tests && python -m pytest -v`
Expected: tüm mevcut testler + yeni `test_qc_converters.py` + `test_process_cpk_reference_csv_matches_formula` PASS, 0 FAIL

- [ ] **Step 6: Commit**

```bash
git add validation/process/ tests/test_validation_suite.py
git commit -m "test: validation/process/ - QC donusturucu formulleri icin Method Validation"
```

---

### Task 7: `METHODOLOGY.md` — Faz 1'i (Brix hariç) "✅ Tamamlandı" olarak işaretle

**Files:**
- Modify: `METHODOLOGY.md`

- [ ] **Step 1: "Faz 1 — Köprü Altyapısı..." başlığını güncelle**

`METHODOLOGY.md`'deki `**Faz 1 — Köprü Altyapısı + 2 modül + Totox bağlantısı**` başlığını `**Faz 1 — Köprü Altyapısı + 2 modül + Totox bağlantısı ✅ Tamamlandı (Brix hariç — bkz. aşağıdaki not)**` yap (v1.4/v1.5/v1.6 başlıklarındaki AYNI "✅ Tamamlandı" deseni) ve şu implementasyon + Brix durumu notunu ekle:

```markdown
  Implementasyon: `src/qc_converters.py` (pure logic — gravimetrik nem,
  köprü fonksiyonu), `src/app.py` "Hızlı Hesaplayıcılar" sekmesi
  (Gravimetrik Nem/Kuru Madde paneli + Totox köprü butonu),
  `validation/process/`.

  **Brix Sıcaklık Düzeltmesi bu tamamlanmaya DAHİL DEĞİL** — kaynak
  (AOAC 932.14c veya USDA AMS resmi tablosu) bu oturumda erişilemedi
  (paywall/bot engelleme, kaynağın güvenilirlik sorunu DEĞİL). Proje
  disiplini gereği ("kaynak doğrulanamazsa sayı uydurulmaz") bu tek
  madde, kaynağa erişildiğinde ayrı bir mini-implementasyonla
  tamamlanacak şekilde bekletiliyor — bkz. implementasyon planı Task 8-9
  (`docs/superpowers/plans/2026-08-19-v1.7-faz1-qc-donusturucu-koprusu.md`).
```

- [ ] **Step 2: Commit**

```bash
git add METHODOLOGY.md
git commit -m "docs: v1.7 Faz 1'i (Brix haric) tamamlandi olarak isaretle"
```

---

## 🔒 BLOCKED — Kaynak erişimi bekleniyor

Aşağıdaki 2 task, Task 1-7'den BAĞIMSIZDIR — Faz 1'in geri kalanı bunlar
olmadan da tamamlanmış ve kullanılabilir kabul edilir. Bu bölüm, AOAC
932.14c veya USDA AMS Brix hidrometre sıcaklık düzeltme tablosuna
(kütüphane aboneliği, satın alma, veya fiziksel kütüphane erişimi ile)
ulaşıldığında yürütülür.

### Task 8: `qc_converters.py` — Brix Sıcaklık Düzeltmesi (kaynak araştırması + formül)

**Bu task'ın ilk adımı kod DEĞİL, araştırmadır** — proje disiplini (Method Validation kuralı) kaynağı doğrulanmamış hiçbir düzeltme katsayısının koda girmesini yasaklar. ICUMSA SPS-4'ün tam metni ücretli/erişilemez olduğu için (bkz. `https://www.icumsa.org/methods/icumsa-specification-and-standard-sps-4-1998/` — özet erişilebilir, tam tablo değil), bu task **iki kabul edilebilir kaynaktan birine** dayanmalıdır:

1. AOAC Official Method 932.14c (Brix hydrometer temperature correction) — kütüphane/AOAC üyeliği üzerinden tam metin.
2. USDA AMS resmi "Temperature Corrections – 20°C Brix Hydrometers" tablosu (`https://www.ams.usda.gov/sites/default/files/media/TemperatureCorrections20CBrixHydrometersXVII.pdf`) — bu oturumda WebFetch ile 403 alındı (bot engelleme), ama tarayıcıdan doğrudan indirilebilir; kamu/resmi kaynaktır.

Bulunamazsa, mevcut projenin **"kaynak doğrulanamazsa sayı uydurulmaz"** disiplini burada da işler: düzeltme tablosu yerine kullanıcı **"Özel/Manuel gir"** ile kendi laboratuvarının kalibrasyon sertifikasındaki düzeltme faktörünü girer (Hammadde Kütüphanesi'ndeki AYNI fallback deseni).

**Files:**
- Modify: `src/constants.py` (yeni `BRIX_TEMPERATURE_CORRECTION_TABLE` sabiti)
- Modify: `src/qc_converters.py`
- Test: `tests/test_qc_converters.py`

**Interfaces:**
- Consumes: `constants.BRIX_TEMPERATURE_CORRECTION_TABLE` (bu task içinde tanımlanır), `qc_converters.build_bridge_subgroup_entry` (Task 2'de tanımlı, DEĞİŞMEZ)
- Produces: `correct_brix_for_temperature(observed_brix: float, temp_c: float, reference_temp_c: float = 20.0) -> dict` — döner `{"corrected_brix": float, "correction_applied": float, "source": str}`

- [ ] **Step 1: Kaynağı doğrula ve `constants.py`'ye tabloyu ekle**

Yukarıdaki 2 kaynaktan birinin tam metnine eriş (kütüphane erişimi, satın alma, veya kurum/üniversite AOAC aboneliği üzerinden). Tablodan, uygulamanın gerçekte kullanacağı aralığı çıkar: **10-30°Bx okuma, 15-30°C ölçüm sıcaklığı, 20°C referans**. `src/constants.py`'ye şu şekilde ekle (mevcut Brix `method_source` alanındaki gibi tam kaynak ismi ve erişim tarihiyle):

```python
# src/constants.py icine eklenir (FOOD_QUALITY_PARAMETER_CONFIG'in ustune,
# mevcut "Brix" urun tablolarinin yanina)

# KAYNAK: [buraya tam kaynak adi + tarih/baski numarasi + erisim tarihi yazilir,
# ornek: "AOAC Official Method 932.14c, 21st ed. (2019), Table X - erisim: YYYY-MM-DD"]
# Yapı: {sicaklik_C: {"per_brix_unit_correction": float}} - referans 20C.
# NOT: Kaynak dogrulanamadan bu sozluk BOS birakilmaz - dogrulanana kadar bu
# task TAMAMLANMIS SAYILMAZ (Method Validation kurali, METHODOLOGY.md).
BRIX_TEMPERATURE_CORRECTION_TABLE = {
    # ... dogrulanmis kaynaktan cikarilan gercek satirlar buraya ...
}
BRIX_CORRECTION_SOURCE = "..."  # tam kaynak metni, method_source alaninda kullanilir
```

- [ ] **Step 2: Write the failing test (kaynaktan alınan gerçek worked example ile)**

Kaynaktan bulunan (uydurulmamış) bir örnek satırla test yaz — örnek iskelet:

```python
# tests/test_qc_converters.py icine eklenir
from qc_converters import correct_brix_for_temperature


def test_correct_brix_for_temperature_matches_source_worked_example():
    # KAYNAK: constants.BRIX_CORRECTION_SOURCE - Step 1'de dogrulanan tablodan
    # birebir alinmis bir satir. Bu testteki sayilar STEP 1 tamamlanmadan
    # yazilamaz (uydurma sayi yasagi, METHODOLOGY.md "Validation zorunlulugu").
    result = correct_brix_for_temperature(observed_brix=..., temp_c=..., reference_temp_c=20.0)
    assert result["corrected_brix"] == pytest.approx(..., abs=0.05)
    assert result["source"] == constants.BRIX_CORRECTION_SOURCE


def test_correct_brix_for_temperature_at_reference_temp_is_noop():
    result = correct_brix_for_temperature(observed_brix=15.0, temp_c=20.0, reference_temp_c=20.0)
    assert result["corrected_brix"] == pytest.approx(15.0, abs=0.001)
    assert result["correction_applied"] == pytest.approx(0.0, abs=0.001)
```

- [ ] **Step 3: Run test to verify it fails**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k brix_temperature`
Expected: FAIL (`ImportError` veya `AttributeError` — fonksiyon henüz yok)

- [ ] **Step 4: Write implementation (Step 1'de doğrulanan tabloya göre enterpolasyon)**

```python
# src/qc_converters.py icine eklenir
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__)))
from constants import BRIX_TEMPERATURE_CORRECTION_TABLE, BRIX_CORRECTION_SOURCE


def correct_brix_for_temperature(
    observed_brix: float, temp_c: float, reference_temp_c: float = 20.0
) -> dict:
    """Olculen Brix degerini referans sicakliga (varsayilan 20C) duzeltir.

    Kaynak: constants.BRIX_CORRECTION_SOURCE (bkz. constants.py yorum satiri
    - tam kaynak referansi ve erisilebilirlik notu orada tutulur).
    """
    if temp_c == reference_temp_c:
        return {"corrected_brix": observed_brix, "correction_applied": 0.0, "source": BRIX_CORRECTION_SOURCE}

    correction = _lookup_brix_correction(temp_c, observed_brix)
    return {
        "corrected_brix": observed_brix + correction,
        "correction_applied": correction,
        "source": BRIX_CORRECTION_SOURCE,
    }


def _lookup_brix_correction(temp_c: float, observed_brix: float) -> float:
    """BRIX_TEMPERATURE_CORRECTION_TABLE uzerinde en yakin nokta enterpolasyonu."""
    # Step 1'de dogrulanan tablo yapisina gore implementasyon burada tamamlanir.
    raise NotImplementedError("Step 1'deki tablo dogrulanip doldurulmadan implemente edilemez")
```

**Not:** `_lookup_brix_correction`'ın gerçek gövdesi, Step 1'de `BRIX_TEMPERATURE_CORRECTION_TABLE`'ın gerçek yapısına (kaç °C artışında, kaç Brix biriminde bir satır olduğuna) bağlıdır — tablo doğrulanmadan bu fonksiyon tamamlanamaz, `NotImplementedError` bilinçli bir durdurma noktasıdır.

- [ ] **Step 5: Run test to verify it passes**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k brix_temperature`
Expected: 2 passed (Step 1-4 tamamlandıktan sonra)

- [ ] **Step 6: `validation/process/cpk_reference.csv`'ye Brix satırını ekle**

Task 6'da oluşturulan CSV'ye, Step 1'de doğrulanan kaynaktan birebir alınan bir worked example satırı eklenir (aynı 10 sütunlu şema).

- [ ] **Step 7: Commit**

```bash
git add src/qc_converters.py src/constants.py tests/test_qc_converters.py validation/process/cpk_reference.csv
git commit -m "feat: Brix sicaklik duzeltmesi (kaynak: BRIX_CORRECTION_SOURCE)"
```

---

### Task 9: `app.py` — "SPC Veri Setine Aktar" UI: Brix Düzeltmesi paneli

**Files:**
- Modify: `src/app.py` (SEKME 3 "Hızlı Hesaplayıcılar", `tab_calc` bloğunun EN BAŞI — Gravimetrik Nem panelinden bile önce, ilk sıraya konur)

**Interfaces:**
- Consumes: `qc_converters.correct_brix_for_temperature(observed_brix, temp_c, reference_temp_c)` (Task 8'de tanımlı), `qc_converters.build_bridge_subgroup_entry(value, shift_label)` (Task 2'de tanımlı), mevcut `st.session_state.subgroups` listesi (`app.py:96-97`)

- [ ] **Step 1: UI panelini ekle**

`app.py`'nin import bloğuna ekle:

```python
from qc_converters import correct_brix_for_temperature
from constants import BRIX_CORRECTION_SOURCE
```

`tab_calc` bloğunun en başına (Gravimetrik Nem panelinden ÖNCE):

```python
with tab_calc:
    st.markdown("### 🌡️ Brix Sıcaklık Düzeltmesi (ICUMSA)")
    st.caption(
        "Ölçülen Brix değerini referans sıcaklığa (20°C) düzeltir. "
        f"Kaynak: {BRIX_CORRECTION_SOURCE}"
    )
    col_brix1, col_brix2 = st.columns(2)
    with col_brix1:
        brix_observed = st.number_input(
            "Ölçülen Brix (°Bx)", min_value=0.0, max_value=100.0,
            value=15.0, step=0.1, key="qc_brix_observed",
        )
    with col_brix2:
        brix_temp = st.number_input(
            "Ölçüm sıcaklığı (°C)", min_value=0.0, max_value=100.0,
            value=20.0, step=0.5, key="qc_brix_temp",
        )

    brix_result = correct_brix_for_temperature(brix_observed, brix_temp)
    st.metric("Düzeltilmiş Brix (20°C)", f"{brix_result['corrected_brix']:.2f} °Bx")
    if brix_result["correction_applied"] != 0.0:
        st.caption(f"Uygulanan düzeltme: {brix_result['correction_applied']:+.3f} °Bx")

    if st.button("📌 SPC Veri Setine Aktar (Brix)", key="qc_brix_bridge_button"):
        entry = build_bridge_subgroup_entry(
            value=brix_result["corrected_brix"],
            shift_label=f"QC Dönüştürücü - Brix ({brix_temp:.1f}°C→20°C)",
        )
        st.session_state.subgroups.append(entry)
        st.success(
            "Düzeltilmiş Brix değeri SPC veri setine eklendi. "
            "Chart & Cpk sekmesinde Parametre olarak 'Brix' seçili olduğundan emin olun."
        )

    st.divider()
```

- [ ] **Step 2: Manuel doğrulama (Streamlit test edilemeyen UI kodu için)**

Run: `cd src && streamlit run app.py`

Tarayıcıda: "Hızlı Hesaplayıcılar" sekmesine git → Brix=15.0, sıcaklık=25.0 gir → düzeltilmiş değer görünmeli → "SPC Veri Setine Aktar" butonuna bas → başarı mesajı görünmeli → "Veri Girişi" sekmesine dön, Parametre="Brix" seçiliyse yeni satırın `st.session_state.subgroups`'a eklendiğini (ham veri tablosunda) doğrula.

- [ ] **Step 3: `METHODOLOGY.md`'deki Brix bekleme notunu kaldır, Faz 1'i tam "✅ Tamamlandı" yap**

Task 7'de eklenen "**Brix Sıcaklık Düzeltmesi bu tamamlanmaya DAHİL DEĞİL**" paragrafı silinir, başlıktaki "(Brix hariç — bkz. aşağıdaki not)" ibaresi kaldırılır.

- [ ] **Step 4: Commit**

```bash
git add src/app.py METHODOLOGY.md
git commit -m "feat: Brix Duzeltmesi UI paneli + SPC koprusu - v1.7 Faz 1 tam tamamlandi"
```

---

## Self-Review Notu (plan yazarı için)

- **Spec coverage:** METHODOLOGY.md'deki Faz 1'in 3 maddesi (Köprü, Brix, Gravimetrik Nem) + Totox bağlantısı → Task 1-2 (köprü+nem), Task 3+5 (Totox), Task 8-9 (Brix, BLOCKED). Hepsi kapsandı.
- **Bilinçli placeholder:** Task 8'deki Brix düzeltme tablosu, gerçek bir kaynak-doğrulama adımı olarak bırakıldı (proje disiplini gereği sayı uydurulamaz) — bu bir plan eksikliği değil, METHODOLOGY.md'nin kendi "Method Validation kuralı"nın doğal sonucu. Task tamamlanmadan (`NotImplementedError` kaldırılmadan) o iki task kapanmış sayılmaz; ama Task 1-7 bağımsız olarak tamamlanıp release edilebilir.
- **Tip tutarlılığı:** `build_bridge_subgroup_entry` dönüş şekli (`{"shift": str, "values": [float]}`) Task 2'de tanımlandı, Task 4/5/9'da birebir aynı şekilde tüketiliyor — kontrol edildi.

---

⬅ [METHODOLOGY.md'ye dön](../../../METHODOLOGY.md)
