# v1.7 Faz 2 — Titrimetrik Dönüştürücüler Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SPC FoodLab'e iki yeni QC Veri Dönüştürücü eklemek — Titre Edilebilir Asitlik ve Tuz (Mohr Metodu) — ve bunları X-bar/R hedeflere (Titrasyon Asitliği, Tuz/NaCl — ikisi de v1.0'dan beri X-bar/R, n=4 varsayılan) gerçek bir alt grup (n adet titrasyon tekrarı) olarak bağlayabilmek için Faz 1'in köprü altyapısını genişletmek.

**Architecture:** Faz 1'de kurulan köprü (`build_bridge_subgroup_entry()` + `render_bridge_widget()`) sadece I-MR (tekli değer) hedeflere izin veriyordu — bu, final review'ın yakaladığı "X-bar/R'a n=1 alt grup eklemek Range'i sıfırlar" bug'ının kalıcı çözümüydü. Faz 2'nin hedefleri (Titrasyon Asitliği, Tuz/NaCl) X-bar/R olduğu için köprü, TEK değer yerine **n değerlik gerçek bir alt grup listesi** kabul edecek şekilde genişletiliyor — bu, gerçek lab pratiğine uygun (aynı numuneyi n kez titre etmek), sahte/tekrarlanan veri değil. `src/qc_converters.py`'ye iki yeni saf-mantık formülü (AOAC titre edilebilir asitlik, Mohr yöntemi tuz tayini) eklenir; katsayılar (asit meq faktörleri, NaCl meq faktörü) IUPAC standart atomik ağırlıklarından türetilir ve türetme `constants.py`'de yorum olarak gösterilir — kaynağı doğrulanamayan bir regülasyon tablosu DEĞİL, birinci-ilke stokiyometri (Cpk/Ppk formüllerinin "ders kitabı matematiği" statüsüyle aynı kategori).

**Tech Stack:** Python, Streamlit, pytest, pandas (validation CSV okuma) — mevcut stack, yeni bağımlılık yok.

**Spec:** `METHODOLOGY.md` → "v1.7 — QC Veri Dönüştürücüler (fazlı)" bölümü, "Faz 2 — Titrimetrik Dönüştürücüler" alt maddesi. Ayrıca bu plan, Faz 1'in `docs/superpowers/plans/2026-08-19-v1.7-faz1-qc-donusturucu-koprusu.md` planındaki final review bulgularının (özellikle X-bar/R gating) üzerine inşa eder — o planın Task 4/5'i ve final-review fix dalgası (`render_bridge_widget`'ın mevcut hali) bu planın ön koşuludur.

## Global Constraints

- **Altın Kural:** her yeni dönüştürücü çıktısını SPC kontrol kartının Y eksenine (mevcut bir parametrenin `subgroups` verisine) bağlamalıdır.
- **Method Validation kuralı:** her yeni formül, literatür veya birinci-ilke türetimiyle doğrulanmış bir worked example ile `validation/process/` altına eklenip test edilmeden release edilmez.
- **No-fabrication disiplini:** kaynağı doğrulanamayan hiçbir sayı uydurulmaz. Asit meq faktörleri ve NaCl meq faktörü BİRİNCİ-İLKE türetimdir (Eş değer ağırlık = Molekül Ağırlığı / bazisite; atomik ağırlıklar IUPAC standardı) — bu, ICUMSA Brix tablosu gibi erişilemez bir regülasyon kaynağı DEĞİL, her zaman yeniden hesaplanabilir bir gerçektir; yine de kullanıcı "Özel/Manuel gir" ile kendi laboratuvarının faktörünü girebilmelidir (Hammadde Kütüphanesi'ndeki AYNI fallback deseni).
- **X-bar/R köprü koruması (Faz 1 final review'dan kalıcı kural):** bir X-bar/R hedefe köprüleme, hedefin güncel alt grup büyüklüğü (`st.session_state.subgroup_size`) ile TAM olarak eşleşen sayıda değer içermelidir — eksik/fazla sayıda değer köprülenemez, buton hiç render edilmez.
- **Mevcut kod SIFIR regresyon:** `spc_core.py`, `compute_cpk()`, X-bar/R ve I-MR motorlarına bu fazda hiçbir değişiklik yapılmaz. Faz 1'in Gravimetrik Nem ve Totox panelleri (ikisi de I-MR, tek değer) DAVRANIŞ OLARAK değişmemelidir — `build_bridge_subgroup_entry(value: float, ...)` çağrıları eskisi gibi çalışmaya devam etmeli.

---

### Task 1: `qc_converters.py` — `build_bridge_subgroup_entry()`'i X-bar/R alt grup listesi kabul edecek şekilde genişlet

**Files:**
- Modify: `src/qc_converters.py`
- Test: `tests/test_qc_converters.py`

**Interfaces:**
- Consumes: yok
- Produces: `build_bridge_subgroup_entry(value: float | list[float], shift_label: str) -> dict` — `value` bir `float` ise eskisi gibi `{"shift": shift_label, "values": [value]}` döner (Faz 1 davranışı DEĞİŞMEZ); `value` bir `list[float]` ise `{"shift": shift_label, "values": list(value)}` döner (yeni X-bar/R yolu). Boş liste veya NaN/inf içeren herhangi bir eleman `ValueError` fırlatır.

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_qc_converters.py icine eklenir

def test_build_bridge_subgroup_entry_accepts_list_for_xbar_r():
    entry = build_bridge_subgroup_entry(
        value=[1.1, 1.2, 1.0, 1.3], shift_label="QC Donusturucu - Test XR",
    )
    assert entry == {"shift": "QC Donusturucu - Test XR", "values": [1.1, 1.2, 1.0, 1.3]}


def test_build_bridge_subgroup_entry_rejects_empty_list():
    with pytest.raises(ValueError, match="bos olamaz"):
        build_bridge_subgroup_entry(value=[], shift_label="QC Donusturucu - Test XR")


def test_build_bridge_subgroup_entry_rejects_non_finite_value_in_list():
    with pytest.raises(ValueError, match="sonlu"):
        build_bridge_subgroup_entry(
            value=[1.0, float("nan"), 1.2], shift_label="QC Donusturucu - Test XR",
        )


def test_build_bridge_subgroup_entry_single_float_still_works():
    # Faz 1 davranisi degismemeli - regresyon kontrolu
    entry = build_bridge_subgroup_entry(value=12.34, shift_label="QC Donusturucu - Test")
    assert entry == {"shift": "QC Donusturucu - Test", "values": [12.34]}
```

- [ ] **Step 2: Run tests to verify the new ones fail**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k "list_for_xbar_r or empty_list or non_finite_value_in_list"`
Expected: FAIL — `isinstance(value, list)` dalı henüz yok, mevcut kod `math.isfinite([1.1, 1.2, ...])` çağırınca `TypeError` fırlatır.

- [ ] **Step 3: Write implementation**

`src/qc_converters.py`'deki `build_bridge_subgroup_entry` fonksiyonunu tamamen şununla değiştir:

```python
def build_bridge_subgroup_entry(value: float | list[float], shift_label: str) -> dict:
    """QC donusturucu sonucunu, mevcut subgroups sema formatina cevirir.

    app.py:96-97'deki st.session_state.subgroups listesi
    {"shift": str, "values": list[float]} sekli bekler; koprubu format
    degistirmez, sadece dogru sekli uretir - spc_core.py'ye HICBIR
    degisiklik gerekmez.

    value bir float ise (I-MR koprusu - tek olcum): {"values": [value]}.
    value bir list[float] ise (X-bar/R koprusu - n adet tekrar olcumu,
    ayni numunenin n kez titre edilmesi gibi gercek bir alt grup): tum
    liste dogrudan {"values": ...} olarak kullanilir. Hedefin X-bar/R
    olup olmadigina ve n'in dogru sayida olup olmadigina bu fonksiyon
    KARAR VERMEZ - o kontrol render_bridge_widget()'ta (app.py) yapilir,
    burasi sadece sekil/gecerlilik kontrolu yapan saf bir donusum katmanidir.
    """
    if isinstance(value, list):
        if not value:
            raise ValueError("kopru degerleri listesi bos olamaz")
        for v in value:
            if not math.isfinite(v):
                raise ValueError("kopru degeri sonlu bir sayi olmalidir (NaN/inf kabul edilmez)")
        return {"shift": shift_label, "values": list(value)}
    if not math.isfinite(value):
        raise ValueError("kopru degeri sonlu bir sayi olmalidir (NaN/inf kabul edilmez)")
    return {"shift": shift_label, "values": [value]}
```

- [ ] **Step 4: Run all qc_converters tests to verify pass + zero regression**

Run: `cd tests && python -m pytest test_qc_converters.py -v`
Expected: all tests pass (Faz 1'in mevcut 3+2+1 testi dahil, artık +4 yeni test).

- [ ] **Step 5: Commit**

```bash
git add src/qc_converters.py tests/test_qc_converters.py
git commit -m "feat: build_bridge_subgroup_entry() X-bar/R alt grup listesini destekler"
```

---

### Task 2: `app.py` — `render_bridge_widget()`'i X-bar/R hedeflere köprüleme yapacak şekilde genişlet

**Files:**
- Modify: `src/app.py` (fonksiyon tanımı, `tab_calc` bloğundan hemen önce — Faz 1'de eklenen `render_bridge_widget` fonksiyonu)

**Interfaces:**
- Consumes: `qc_converters.build_bridge_subgroup_entry(value: float | list[float], shift_label)` (Task 1'de genişletildi), `st.session_state.subgroup_size` (mevcut global n, `app.py:382`'de `subgroup_n = st.session_state.subgroup_size` olarak zaten okunuyor — köprü fonksiyonu doğrudan `st.session_state.subgroup_size`'ı okuyabilir, aynı değer)
- Produces: `render_bridge_widget(values_by_target: dict[str, float | list[float]], source_label: str, widget_key_prefix: str, extra_note: str | None = None) -> None` — imza DEĞİŞMEDİ (geriye dönük uyumlu), ama artık `values_by_target`'taki bir değer `list[float]` ise hedefin X-bar/R olduğunu ve alt grup olarak köprüleneceğini anlar.

- [ ] **Step 1: Mevcut fonksiyonun X-bar/R'ı REDDEDEN bloğunu bul**

`src/app.py`'de `render_bridge_widget` fonksiyonu içindeki şu bloğu ara (Faz 1 final review fix'inden kalma):

```python
    if not target_is_individual:
        st.warning(
            f"'{target}' bir X-bar/R parametresidir (alt grup büyüklüğü n>1). "
            "Bu köprü sadece I-MR (tekli ölçüm) parametrelerine aktarım yapabilir - "
            "tek bir değeri X-bar/R alt grubuna eklemek Range'i yapay olarak sıfırlar "
            "ve Cpk'yı yanıltıcı şekilde şişirir."
        )
        return

    st.caption(f"Aktif parametre: {target} (I-MR)")
    if st.button(f"📌 SPC Veri Setine Aktar ({source_label})", key=f"{widget_key_prefix}_bridge_button"):
        entry = build_bridge_subgroup_entry(
            value=values_by_target[target], shift_label=f"QC Dönüştürücü - {source_label}",
        )
        st.session_state.subgroups.append(entry)
        message = f"{source_label} değeri SPC veri setine eklendi ({target}, I-MR)."
        if extra_note:
            message += " " + extra_note
        st.success(message)
```

- [ ] **Step 2: Bu bloğu tamamen şununla DEĞİŞTİR**

```python
    if not target_is_individual:
        target_values = values_by_target[target]
        if not isinstance(target_values, list):
            target_values = [target_values]
        required_n = st.session_state.subgroup_size
        if len(target_values) != required_n:
            st.warning(
                f"'{target}' bir X-bar/R parametresidir, mevcut alt grup büyüklüğü "
                f"n={required_n}. Bu köprü tam olarak {required_n} ölçüm gerektirir "
                f"({len(target_values)} girildi) - sidebar'dan n'i değiştirin veya "
                "eksik/fazla ölçümü düzeltin."
            )
            return
        st.caption(f"Aktif parametre: {target} (X-bar/R, n={required_n})")
        if st.button(f"📌 SPC Veri Setine Aktar ({source_label})", key=f"{widget_key_prefix}_bridge_button"):
            entry = build_bridge_subgroup_entry(
                value=target_values, shift_label=f"QC Dönüştürücü - {source_label}",
            )
            st.session_state.subgroups.append(entry)
            message = (
                f"{source_label} değeri SPC veri setine eklendi "
                f"({target}, X-bar/R, n={required_n})."
            )
            if extra_note:
                message += " " + extra_note
            st.success(message)
        return

    st.caption(f"Aktif parametre: {target} (I-MR)")
    if st.button(f"📌 SPC Veri Setine Aktar ({source_label})", key=f"{widget_key_prefix}_bridge_button"):
        entry = build_bridge_subgroup_entry(
            value=values_by_target[target], shift_label=f"QC Dönüştürücü - {source_label}",
        )
        st.session_state.subgroups.append(entry)
        message = f"{source_label} değeri SPC veri setine eklendi ({target}, I-MR)."
        if extra_note:
            message += " " + extra_note
        st.success(message)
```

(Not: I-MR dalı DEĞİŞMEDİ, sadece X-bar/R dalı `return`'lü bir erken-çıkış bloğuna dönüştü — iki dal artık birbirinden bağımsız, kod tekrarı okunabilirlik için kabul edilebilir, bkz. Faz 1'deki aynı desen.)

- [ ] **Step 3: Fonksiyonun docstring'ini güncelle**

`render_bridge_widget`'ın docstring'indeki `values_by_target` açıklamasını şuna genişlet (mevcut paragrafın hemen altına ekle, silme):

```
    Faz 2 guncellemesi: values_by_target'taki bir deger artik list[float] de
    olabilir - bu, hedefin X-bar/R oldugunu ve TAM OLARAK
    st.session_state.subgroup_size kadar elemanla (gercek bir alt grup -
    ayni numunenin n kez olculmesi) koprulenecegini belirtir. Eksik/fazla
    sayida deger reddedilir (buton render edilmez) - bu, Faz 1 final
    review'in yakaladigi "X-bar/R'a n=1 alt grup eklemek Range'i sifirlar"
    bug'inin kalici cozumudur.
```

- [ ] **Step 4: Syntax kontrolü + tam regresyon**

Run: `cd src && python -c "import ast; ast.parse(open('app.py', encoding='utf-8').read())"`
Expected: hata yok.

Run: `cd tests && python -m pytest -q`
Expected: mevcut tüm testler geçer (bu adım UI kodu değiştirdi, pytest'in kapsamı dışında ama zero regresyon garantisi için çalıştırılır).

- [ ] **Step 5: Manuel izleme (elle doğrulama, otomatik test edilemeyen UI kodu)**

`render_bridge_widget`'ı çağıran mevcut iki yer (Gravimetrik Nem paneli, Totox köprüsü) hâlâ `float` değer geçiyor — bu yüzden `target_is_individual=True` dalına giriyorlar, DEĞİŞMEDİ. Elle izle: `values_by_target={"Nem/Rutubet": 12.5}` gibi bir çağrı hâlâ eskisi gibi I-MR dalına gider (list değil), davranış aynı kalır. Faz 3'te yazılacak yeni panellerin `list[float]` geçmesi X-bar/R dalını tetikleyecek.

- [ ] **Step 6: Commit**

```bash
git add src/app.py
git commit -m "feat: render_bridge_widget() X-bar/R hedeflere gercek alt grup kopruleyebilir"
```

---

### Task 3: `qc_converters.py` — Titre Edilebilir Asitlik formülü + asit meq faktörleri

**Files:**
- Modify: `src/qc_converters.py`
- Modify: `src/constants.py`
- Test: `tests/test_qc_converters.py`

**Interfaces:**
- Consumes: `constants.TITRATABLE_ACID_MEQ_FACTORS` (bu task içinde tanımlanır)
- Produces: `titratable_acidity(titrant_volume_ml: float, titrant_normality: float, acid_meq_factor: float, sample_size_ml: float) -> dict` — döner `{"acidity_pct": float}`.

- [ ] **Step 1: `constants.py`'ye asit meq faktör tablosunu ekle**

`src/constants.py`'nin sonuna (Totox köprü config'inden sonra) ekle:

```python
# --- Titre Edilebilir Asitlik: Miliekivalan (meq) Faktorleri (v1.7 Faz 2) ---
# Turetme (birinci-ilke stokiyometri, KAYNAK DEGIL - her zaman yeniden
# hesaplanabilir bir gercek, ICUMSA Brix tablosu gibi erisilemez bir
# regulasyon kaynagi DEGIL): Es deger agirlik (g/eq) = Molekul Agirligi (MW)
# / bazisite (H+ sayisi); meq faktoru (g/meq) = Es deger agirlik / 1000.
# Atomik agirliklar: IUPAC standart degerleri (C=12.011, H=1.008, O=15.999).
# - Sitrik Asit (anhidrus, C6H8O7, MW=192.124, triprotik):
#   192.124 / 3 / 1000 = 0.064041 -> 0.0640
# - Malik Asit (C4H6O5, MW=134.087, diprotik):
#   134.087 / 2 / 1000 = 0.067044 -> 0.0670
# - Laktik Asit (C3H6O3, MW=90.078, monoprotik):
#   90.078 / 1000 = 0.090078 -> 0.0900
# - Asetik Asit (C2H4O2, MW=60.052, monoprotik):
#   60.052 / 1000 = 0.060052 -> 0.0600
# Formul: %Asitlik = (titrant_hacmi_mL x titrant_normalitesi x meq_faktoru x 100) / numune_mL
TITRATABLE_ACID_MEQ_FACTORS = {
    "Sitrik Asit (anhidrus)": 0.0640,
    "Malik Asit": 0.0670,
    "Laktik Asit": 0.0900,
    "Asetik Asit": 0.0600,
    "Ozel/Manuel gir": None,
}
```

- [ ] **Step 2: Write the failing test**

```python
# tests/test_qc_converters.py icine eklenir

def test_titratable_acidity_worked_example_citric_acid():
    # Turetilmis meq faktoru (bkz. constants.TITRATABLE_ACID_MEQ_FACTORS
    # yorum satiri): sitrik asit = 0.0640 g/meq
    # V=9.2 mL, N=0.1, faktor=0.064, numune=10.0 mL
    # %TA = (9.2 * 0.1 * 0.064 * 100) / 10.0 = 5.888 / 10.0 = 0.5888
    result = titratable_acidity(
        titrant_volume_ml=9.2, titrant_normality=0.1, acid_meq_factor=0.064, sample_size_ml=10.0,
    )
    assert result["acidity_pct"] == pytest.approx(0.5888, abs=0.0001)


def test_titratable_acidity_zero_sample_size_raises():
    with pytest.raises(ValueError, match="numune miktari"):
        titratable_acidity(
            titrant_volume_ml=9.2, titrant_normality=0.1, acid_meq_factor=0.064, sample_size_ml=0.0,
        )


def test_titratable_acidity_negative_factor_raises():
    with pytest.raises(ValueError, match="asit faktoru"):
        titratable_acidity(
            titrant_volume_ml=9.2, titrant_normality=0.1, acid_meq_factor=-0.064, sample_size_ml=10.0,
        )
```

- [ ] **Step 3: Run test to verify it fails**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k titratable_acidity`
Expected: FAIL with `NameError`/`ImportError` — fonksiyon henüz yok.

- [ ] **Step 4: Write implementation**

`src/qc_converters.py`'nin sonuna ekle:

```python
def titratable_acidity(
    titrant_volume_ml: float, titrant_normality: float, acid_meq_factor: float, sample_size_ml: float,
) -> dict:
    """AOAC titre edilebilir asitlik: %Asitlik = (V x N x meq_faktoru x 100) / numune.

    titrant_volume_ml: harcanan titrant (orn. NaOH) hacmi (mL)
    titrant_normality: titrant normalitesi (N, eq/L)
    acid_meq_factor: baskin asidin miliekivalan faktoru (g/meq) - bkz.
        constants.TITRATABLE_ACID_MEQ_FACTORS turetme notu
    sample_size_ml: numune miktari (mL)
    """
    if sample_size_ml <= 0:
        raise ValueError("numune miktari sifir veya negatif olamaz")
    if titrant_volume_ml < 0 or titrant_normality < 0:
        raise ValueError("titre hacmi/normalite negatif olamaz")
    if acid_meq_factor <= 0:
        raise ValueError("asit faktoru sifir veya negatif olamaz")
    acidity_pct = (titrant_volume_ml * titrant_normality * acid_meq_factor * 100.0) / sample_size_ml
    return {"acidity_pct": acidity_pct}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k titratable_acidity`
Expected: 3 passed

- [ ] **Step 6: Commit**

```bash
git add src/qc_converters.py src/constants.py tests/test_qc_converters.py
git commit -m "feat: titre edilebilir asitlik formulu + asit meq faktorleri (turetilmis)"
```

---

### Task 4: `qc_converters.py` — Tuz (Mohr Metodu) formülü + NaCl meq faktörü

**Files:**
- Modify: `src/qc_converters.py`
- Test: `tests/test_qc_converters.py`

**Interfaces:**
- Consumes: yok (NaCl faktörü sabit, ürün/asit seçimine göre değişmez)
- Produces: `salt_content_mohr(titrant_volume_ml: float, titrant_normality: float, sample_size_g: float) -> dict` — döner `{"salt_pct": float}`. `NACL_MEQ_FACTOR = 0.05844` modül seviyesinde sabit olarak tanımlanır.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_qc_converters.py icine eklenir

def test_salt_content_mohr_worked_example():
    # Turetilmis meq faktoru: NaCl (MW=58.44 g/mol, tek degerlikli) ->
    # meq faktoru = 58.44 / 1000 = 0.05844 g/meq
    # V=12.0 mL AgNO3, N=0.1, numune=10.0 g
    # %NaCl = (12.0 * 0.1 * 0.05844 * 100) / 10.0 = 7.0128 / 10.0 = 0.70128
    result = salt_content_mohr(titrant_volume_ml=12.0, titrant_normality=0.1, sample_size_g=10.0)
    assert result["salt_pct"] == pytest.approx(0.70128, abs=0.0001)


def test_salt_content_mohr_zero_sample_raises():
    with pytest.raises(ValueError, match="numune agirligi"):
        salt_content_mohr(titrant_volume_ml=12.0, titrant_normality=0.1, sample_size_g=0.0)


def test_nacl_meq_factor_matches_derivation():
    # Dogrulama: MW(NaCl)=22.990(Na)+35.453(Cl)=58.443 (IUPAC atomik agirliklar)
    assert NACL_MEQ_FACTOR == pytest.approx(58.44 / 1000, abs=0.0001)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k "salt_content_mohr or nacl_meq_factor"`
Expected: FAIL with `NameError`/`ImportError`.

- [ ] **Step 3: Write implementation**

`src/qc_converters.py`'nin sonuna ekle:

```python
# NaCl meq faktoru: MW(NaCl) = 22.990(Na) + 35.453(Cl) = 58.443 g/mol
# (IUPAC standart atomik agirliklari), tek degerlikli (bazisite=1) ->
# meq faktoru = MW/1000 = 0.058443 -> 0.05844 (Mohr yontemi, AgNO3
# titrasyonu ile klorur tayini, %NaCl olarak raporlanir).
NACL_MEQ_FACTOR = 0.05844


def salt_content_mohr(
    titrant_volume_ml: float, titrant_normality: float, sample_size_g: float,
) -> dict:
    """Mohr yontemi (AgNO3 titrasyonu ile klorur tayini): %NaCl = (V x N x 0.05844 x 100) / numune(g).

    titrant_volume_ml: harcanan AgNO3 hacmi (mL)
    titrant_normality: AgNO3 normalitesi (N, eq/L)
    sample_size_g: numune agirligi (g)
    """
    if sample_size_g <= 0:
        raise ValueError("numune agirligi sifir veya negatif olamaz")
    if titrant_volume_ml < 0 or titrant_normality < 0:
        raise ValueError("titre hacmi/normalite negatif olamaz")
    salt_pct = (titrant_volume_ml * titrant_normality * NACL_MEQ_FACTOR * 100.0) / sample_size_g
    return {"salt_pct": salt_pct}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_qc_converters.py -v -k "salt_content_mohr or nacl_meq_factor"`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add src/qc_converters.py tests/test_qc_converters.py
git commit -m "feat: tuz (Mohr metodu) formulu + NaCl meq faktoru (turetilmis)"
```

---

### Task 5: `app.py` — Titre Edilebilir Asitlik UI paneli (n-tekrarlı X-bar/R köprüsü)

**Files:**
- Modify: `src/app.py` (`tab_calc` bloğu içine, Gravimetrik Nem panelinden SONRA, Totox bölümünden ÖNCE — yeni bir alt-bölüm)

**Interfaces:**
- Consumes: `qc_converters.titratable_acidity(...)` (Task 3), `constants.TITRATABLE_ACID_MEQ_FACTORS` (Task 3), `render_bridge_widget(values_by_target: dict[str, list[float]], ...)` (Task 2'de X-bar/R desteği eklendi), `st.session_state.subgroup_size`

- [ ] **Step 1: Import satırlarını güncelle**

`app.py`'nin import bloğuna ekle (mevcut `from qc_converters import (...)` çok satırlı bloğu varsa oraya, yoksa Faz 1'deki tekli import satırının yanına):

```python
from qc_converters import titratable_acidity
from constants import TITRATABLE_ACID_MEQ_FACTORS
```

- [ ] **Step 2: UI panelini ekle**

Gravimetrik Nem panelinin (Faz 1'den, mevcut kod) bitişindeki `st.divider()`'ından hemen sonra, Totox `with st.container(border=True, key="card-24"):` bloğundan ÖNCE ekle:

```python
    st.markdown("### 🧪 Titre Edilebilir Asitlik")
    st.caption(
        "AOAC yöntemi: titre hacmi + normalite + asit faktörü + numune "
        "miktarından % asitlik hesaplar. Titrasyon Asitliği X-bar/R "
        "parametresi olduğu için, aktif alt grup büyüklüğü (n) kadar "
        "tekrar ölçüm gerekir - aynı numunenin n kez titre edilmesi gibi."
    )

    col_ta1, col_ta2, col_ta3 = st.columns(3)
    with col_ta1:
        ta_acid_choice = st.selectbox(
            "Baskın asit", list(TITRATABLE_ACID_MEQ_FACTORS.keys()), key="qc_ta_acid_choice",
        )
    ta_acid_factor = TITRATABLE_ACID_MEQ_FACTORS[ta_acid_choice]
    with col_ta2:
        if ta_acid_factor is None:
            ta_acid_factor = st.number_input(
                "Özel asit faktörü (g/meq)", min_value=0.0001, value=0.0640,
                step=0.0001, format="%.4f", key="qc_ta_custom_factor",
            )
        else:
            st.metric("Asit faktörü (g/meq)", f"{ta_acid_factor:.4f}")
    with col_ta3:
        ta_normality = st.number_input(
            "Titrant normalitesi (N)", min_value=0.0001, value=0.1,
            step=0.01, format="%.3f", key="qc_ta_normality",
        )

    ta_sample_size = st.number_input(
        "Numune miktarı (mL)", min_value=0.0001, value=10.0, step=0.1, key="qc_ta_sample_size",
    )

    ta_n = st.session_state.subgroup_size
    st.caption(f"Alt grup büyüklüğü n={ta_n} (sidebar'dan değiştirilebilir) - {ta_n} adet titrasyon tekrarı girin.")

    ta_values = []
    ta_error = None
    ta_cols = st.columns(ta_n)
    for _ta_i, _ta_col in enumerate(ta_cols):
        with _ta_col:
            _ta_vol = st.number_input(
                f"Titre hacmi #{_ta_i + 1} (mL)", min_value=0.0, value=9.2,
                step=0.1, key=f"qc_ta_volume_{_ta_i}",
            )
            try:
                _ta_result = titratable_acidity(_ta_vol, ta_normality, ta_acid_factor, ta_sample_size)
                ta_values.append(_ta_result["acidity_pct"])
            except ValueError as exc:
                ta_error = str(exc)

    if ta_error:
        st.error(f"Girdi hatası: {ta_error}")
    else:
        st.write(
            "Hesaplanan % asitlik değerleri: "
            + ", ".join(f"{v:.3f}" for v in ta_values)
        )
        render_bridge_widget(
            values_by_target={"Titrasyon Asitligi": ta_values},
            source_label="Titre Edilebilir Asitlik",
            widget_key_prefix="qc_ta",
        )

    st.divider()
```

- [ ] **Step 3: Syntax kontrolü + tam regresyon**

Run: `cd src && python -c "import ast; ast.parse(open('app.py', encoding='utf-8').read())"`
Expected: hata yok.

Run: `cd tests && python -m pytest -q`
Expected: mevcut tüm testler geçer, sıfır regresyon.

- [ ] **Step 4: Manuel doğrulama**

Run: `cd src && streamlit run app.py`

Tarayıcıda: "Hızlı Hesaplayıcılar" sekmesine git → aktif parametreyi "Titrasyon Asitliği" yap (Veri Girişi sekmesinden) → Titre Edilebilir Asitlik panelinde n=4 (varsayılan) adet titre hacmi kutusu görünmeli → her birine değer gir → "SPC Veri Setine Aktar" butonu görünmeli (n eşleşince) → tıkla → başarı mesajı → Veri Girişi sekmesinde yeni bir alt grubun (4 değerli) eklendiğini doğrula. Ardından n'i sidebar'dan 3'e değiştir, panelin 3 kutuya indiğini ve buton mesajının güncellendiğini doğrula.

- [ ] **Step 5: Commit**

```bash
git add src/app.py
git commit -m "feat: Titre Edilebilir Asitlik UI paneli (n-tekrarli X-bar/R koprusu)"
```

---

### Task 6: `app.py` — Tuz (Mohr Metodu) UI paneli (n-tekrarlı X-bar/R köprüsü)

**Files:**
- Modify: `src/app.py` (Task 5'in eklediği bölümden hemen sonra, aynı `tab_calc` içinde, Totox bölümünden ÖNCE)

**Interfaces:**
- Consumes: `qc_converters.salt_content_mohr(...)` (Task 4), `render_bridge_widget(values_by_target: dict[str, list[float]], ...)`, `st.session_state.subgroup_size`

- [ ] **Step 1: Import satırını güncelle**

`app.py`'nin import bloğuna ekle:

```python
from qc_converters import salt_content_mohr
```

- [ ] **Step 2: UI panelini ekle**

```python
    st.markdown("### 🧂 Tuz (Mohr Metodu)")
    st.caption(
        "AgNO₃ titrasyonu ile klorür tayini, %NaCl olarak raporlanır. "
        "Tuz/NaCl X-bar/R parametresi olduğu için aktif alt grup büyüklüğü "
        "(n) kadar tekrar ölçüm gerekir."
    )

    col_salt1, col_salt2 = st.columns(2)
    with col_salt1:
        salt_normality = st.number_input(
            "AgNO₃ normalitesi (N)", min_value=0.0001, value=0.1,
            step=0.01, format="%.3f", key="qc_salt_normality",
        )
    with col_salt2:
        salt_sample_size = st.number_input(
            "Numune miktarı (g)", min_value=0.0001, value=10.0, step=0.1, key="qc_salt_sample_size",
        )

    salt_n = st.session_state.subgroup_size
    st.caption(f"Alt grup büyüklüğü n={salt_n} (sidebar'dan değiştirilebilir) - {salt_n} adet titrasyon tekrarı girin.")

    salt_values = []
    salt_error = None
    salt_cols = st.columns(salt_n)
    for _salt_i, _salt_col in enumerate(salt_cols):
        with _salt_col:
            _salt_vol = st.number_input(
                f"AgNO₃ hacmi #{_salt_i + 1} (mL)", min_value=0.0, value=12.0,
                step=0.1, key=f"qc_salt_volume_{_salt_i}",
            )
            try:
                _salt_result = salt_content_mohr(_salt_vol, salt_normality, salt_sample_size)
                salt_values.append(_salt_result["salt_pct"])
            except ValueError as exc:
                salt_error = str(exc)

    if salt_error:
        st.error(f"Girdi hatası: {salt_error}")
    else:
        st.write(
            "Hesaplanan % NaCl değerleri: "
            + ", ".join(f"{v:.3f}" for v in salt_values)
        )
        render_bridge_widget(
            values_by_target={"Tuz/NaCl": salt_values},
            source_label="Tuz (Mohr Metodu)",
            widget_key_prefix="qc_salt",
        )

    st.divider()
```

- [ ] **Step 3: Syntax kontrolü + tam regresyon**

Run: `cd src && python -c "import ast; ast.parse(open('app.py', encoding='utf-8').read())"`
Run: `cd tests && python -m pytest -q`
Expected: hata yok, sıfır regresyon.

- [ ] **Step 4: Manuel doğrulama**

Aynı akış: aktif parametreyi "Tuz/NaCl" yap → n kadar AgNO₃ hacmi kutusu → değer gir → köprüle → Veri Girişi sekmesinde doğrula.

- [ ] **Step 5: Commit**

```bash
git add src/app.py
git commit -m "feat: Tuz (Mohr Metodu) UI paneli (n-tekrarli X-bar/R koprusu)"
```

---

### Task 7: `validation/process/` — Titre Edilebilir Asitlik ve Tuz formülleri için Method Validation

Faz 1'in final review'ı (Important #4), `cpk_reference.csv`'nin `gravimetric_moisture()`'ı DEĞİL, `compute_cpk()`'ı doğruladığını tespit etmişti — bu fazda, YENİ formüllerin (titratable_acidity, salt_content_mohr) kendisini doğrudan doğrulayan, dürüst bir validation dosyası açılıyor.

**Files:**
- Create: `validation/process/titration_reference.csv`
- Modify: `validation/process/README.md`
- Modify: `tests/test_validation_suite.py`

**Interfaces:**
- Consumes: `qc_converters.titratable_acidity`, `qc_converters.salt_content_mohr`

- [ ] **Step 1: `validation/process/titration_reference.csv` oluştur**

```csv
source,formula,titrant_volume_ml,titrant_normality,factor,sample_size,expected_pct,tolerance,notes
"Turetilmis: MW(sitrik asit anhidrus C6H8O7)=192.124 g/mol (IUPAC atomik agirliklari), triprotik -> Eq=192.124/3=64.041 g/eq -> meq faktoru=0.064041 (yuvarlanmis 0.0640)",titratable_acidity,9.2,0.1,0.0640,10.0,0.5888,0.001,"Formul: %TA=(V*N*faktor*100)/numune"
"Turetilmis: MW(NaCl)=22.990(Na)+35.453(Cl)=58.443 g/mol (IUPAC atomik agirliklari), tek degerlikli -> meq faktoru=58.443/1000=0.058443 (yuvarlanmis 0.05844)",salt_content_mohr,12.0,0.1,0.05844,10.0,0.70128,0.001,"Formul: %NaCl=(V*N*0.05844*100)/numune(g); Mohr yontemi AgNO3 titrasyonu"
```

- [ ] **Step 2: `validation/process/README.md`'yi güncelle**

Mevcut dosyanın sonuna ekle (üzerine yazma, ekle):

```markdown

## titration_reference.csv (v1.7 Faz 2)

`cpk_reference.csv`'den FARKLI olarak (bkz. yukarıdaki not — o sadece
`compute_cpk()`'ı doğrular), bu dosya `titratable_acidity()` ve
`salt_content_mohr()` formüllerinin KENDİSİNİ doğrudan doğrular. Her satır,
`formula` sütununa göre ilgili fonksiyonu çağırır ve sonucu `expected_pct`
ile karşılaştırır.

Katsayılar (`factor` sütunu) dış bir regülasyon kaynağından DEĞİL, birinci-
ilke stokiyometriden (Eş değer ağırlık = Molekül Ağırlığı / bazisite,
IUPAC standart atomik ağırlıkları) türetilmiştir — `source` sütununda tam
türetme gösterilir. Bu, ICUMSA Brix tablosu gibi erişilemez bir kaynak
DEĞİLDİR; her zaman yeniden hesaplanabilir bir gerçektir.
```

- [ ] **Step 3: `test_validation_suite.py`'ye yeni test fonksiyonu ekle**

Dosyanın üst kısmındaki import bloğuna ekle (mevcut importların yanına):

```python
from qc_converters import salt_content_mohr, titratable_acidity
```

Dosyanın sonuna ekle:

```python
def test_process_titration_reference_csv_matches_formulas():
    df = _load("titration_reference.csv", subdir="process")
    for _, row in df.iterrows():
        if row["formula"] == "titratable_acidity":
            result = titratable_acidity(
                titrant_volume_ml=row["titrant_volume_ml"],
                titrant_normality=row["titrant_normality"],
                acid_meq_factor=row["factor"],
                sample_size_ml=row["sample_size"],
            )
            actual = result["acidity_pct"]
        elif row["formula"] == "salt_content_mohr":
            result = salt_content_mohr(
                titrant_volume_ml=row["titrant_volume_ml"],
                titrant_normality=row["titrant_normality"],
                sample_size_g=row["sample_size"],
            )
            actual = result["salt_pct"]
        else:
            raise AssertionError(f"Bilinmeyen formul: {row['formula']}")

        assert abs(actual - row["expected_pct"]) <= row["tolerance"], (
            f"{row['formula']} uyusmuyor: hesaplanan={actual}, "
            f"beklenen={row['expected_pct']} (kaynak: {row['source']})"
        )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tests && python -m pytest test_validation_suite.py -v -k titration`
Expected: 1 passed

- [ ] **Step 5: Tüm test paketini çalıştır (regresyon kontrolü)**

Run: `cd tests && python -m pytest -q`
Expected: tüm mevcut testler + bu fazın yeni testleri PASS, 0 FAIL.

- [ ] **Step 6: Commit**

```bash
git add validation/process/titration_reference.csv validation/process/README.md tests/test_validation_suite.py
git commit -m "test: validation/process/titration_reference.csv - titrimetrik formul dogrulamasi"
```

---

### Task 8: `METHODOLOGY.md` — Faz 2'yi tamamlandı olarak işaretle

**Files:**
- Modify: `METHODOLOGY.md`

- [ ] **Step 1: "Faz 2 — Titrimetrik Dönüştürücüler" başlığını güncelle**

`METHODOLOGY.md`'deki `**Faz 2 — Titrimetrik Dönüştürücüler**` başlığını `**Faz 2 — Titrimetrik Dönüştürücüler ✅ Tamamlandı**` yap ve maddelerin altına şu implementasyon notunu ekle:

```markdown

Implementasyon: `src/qc_converters.py` (`titratable_acidity()`,
`salt_content_mohr()` — katsayılar IUPAC atomik ağırlıklarından
türetilmiş, kaynağı doğrulanamayan bir tablo DEĞİL), `src/app.py`
"Hızlı Hesaplayıcılar" sekmesi (iki yeni panel, n-tekrarlı X-bar/R
köprüsü), `validation/process/titration_reference.csv`.

**Mimari not:** bu faz, Faz 1'in köprü altyapısını (`build_bridge_
subgroup_entry()`, `render_bridge_widget()`) X-bar/R hedefleri
destekleyecek şekilde genişletti — artık bir köprü, hedefin güncel alt
grup büyüklüğü (n) kadar gerçek tekrar ölçümü (aynı numunenin n kez
titre edilmesi) topluyorsa X-bar/R parametrelerine de bağlanabilir.
Faz 1'deki I-MR-only kısıtlaması (final review bulgusu) hâlâ geçerlidir
— sadece tam sayıda değer verildiğinde X-bar/R köprüsü açılır, n=1 alt
grup asla eklenmez.
```

- [ ] **Step 2: Commit**

```bash
git add METHODOLOGY.md
git commit -m "docs: v1.7 Faz 2'yi tamamlandi olarak isaretle"
```

---

## Self-Review Notu (plan yazarı için)

- **Spec coverage:** METHODOLOGY.md'deki Faz 2'nin 2 maddesi (Titre Edilebilir Asitlik, Tuz/Mohr) → Task 3+5 (asitlik), Task 4+6 (tuz). Köprü genişletmesi (kullanıcının seçtiği "X-bar/R'a geniş" yaklaşım) → Task 1+2. Validation → Task 7. Docs → Task 8. Hepsi kapsandı.
- **Bilinçli tasarım kararı:** asit/NaCl meq faktörleri birinci-ilke stokiyometriden türetildi (Brix'teki gibi erişilemez bir kaynak beklenmedi) — bu, no-fabrication disiplinini ihlal etmez çünkü türetme her zaman yeniden yapılabilir/doğrulanabilir bir gerçektir (Cpk formülünün kendisi gibi), keyfi bir katsayı değildir. "Özel/Manuel gir" fallback'i yine de kullanıcının kendi asit hidrat formu (örn. sitrik asit monohidrat 0.070 vs anhidrus 0.064) farklıysa düzeltme yapabilmesini sağlar.
- **Tip tutarlılığı:** `build_bridge_subgroup_entry(value: float | list[float], ...)` Task 1'de tanımlandı; `render_bridge_widget`'ın X-bar/R dalı (Task 2) bunu `list[float]` ile çağırıyor, I-MR dalı (Faz 1'den değişmedi) `float` ile çağırmaya devam ediyor — Task 5/6'daki paneller `values_by_target={"...": [v1, v2, ...]}` (liste) geçiyor, tutarlı.
- **X-bar/R koruması kalıcılığı:** Task 2'deki `len(target_values) != required_n` kontrolü, Faz 1 final review'ın kalıcı kuralını (asla n≠doğru sayıda değer köprülenemez) yeni panellere de otomatik uygular — panel kodu (Task 5/6) kendi gating mantığını YAZMIYOR, sadece `render_bridge_widget`'a doğru şekilli veri geçiyor.

---

⬅ [METHODOLOGY.md'ye dön](../../../METHODOLOGY.md)
